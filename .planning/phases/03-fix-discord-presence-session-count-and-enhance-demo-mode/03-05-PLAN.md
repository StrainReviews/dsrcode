---
phase: 03-fix-discord-presence-session-count-and-enhance-demo-mode
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - skills/demo/SKILL.md
  - CHANGELOG.md
  - README.md
  - main.go
  - scripts/start.sh
  - .claude-plugin/plugin.json
autonomous: false
requirements: [D-09, D-12, D-18, D-19, D-22]

must_haves:
  truths:
    - "Demo skill has 4 modes: Quick Preview, Preset Tour, Multi-Session Preview, Message Rotation"
    - "Auto-cycle is skill-driven with user confirmation per preset (D-12)"
    - "CHANGELOG.md has v3.1.0 entry with Fixed/Added/Changed sections (D-19)"
    - "README.md demo section documents 4 modes and API (D-22)"
    - "Version is v3.1.0 in main.go, plugin.json, start.sh (D-18)"
  artifacts:
    - path: "skills/demo/SKILL.md"
      provides: "4-mode demo skill with menu and instructions"
      contains: "Quick Preview"
    - path: "CHANGELOG.md"
      provides: "v3.1.0 changelog entry"
      contains: "[3.1.0]"
    - path: "README.md"
      provides: "Demo section with 4 modes"
      contains: "Multi-Session Preview"
    - path: "main.go"
      provides: "Version 3.1.0"
      contains: "3.1.0"
    - path: "scripts/start.sh"
      provides: "VERSION v3.1.0"
      contains: "v3.1.0"
    - path: ".claude-plugin/plugin.json"
      provides: "Version 3.1.0"
      contains: "3.1.0"
  key_links:
    - from: "skills/demo/SKILL.md"
      to: "server/server.go POST /preview"
      via: "curl commands in skill instructions"
      pattern: "POST.*preview"
    - from: "skills/demo/SKILL.md"
      to: "server/server.go GET /preview/messages"
      via: "curl commands for message rotation"
      pattern: "GET.*preview/messages"
---

<objective>
Rewrite the demo skill with 4 modes and update version/changelog/README to v3.1.0.

Purpose: Complete the user-facing deliverables: a rich demo experience with full Discord Activity control, proper versioning, changelog for release notes, and README documentation for marketplace visibility.

Output: Rewritten SKILL.md, updated CHANGELOG.md, README.md demo section, version bump across all files.
</objective>

<execution_context>
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-fix-discord-presence-session-count-and-enhance-demo-mode/03-CONTEXT.md
@.planning/phases/03-fix-discord-presence-session-count-and-enhance-demo-mode/03-04-SUMMARY.md

Source files in C:\Users\ktown\Projects\cc-discord-presence:
@C:\Users\ktown\Projects\cc-discord-presence\skills\demo\SKILL.md
@C:\Users\ktown\Projects\cc-discord-presence\CHANGELOG.md
@C:\Users\ktown\Projects\cc-discord-presence\README.md
@C:\Users\ktown\Projects\cc-discord-presence\main.go
@C:\Users\ktown\Projects\cc-discord-presence\scripts\start.sh
@C:\Users\ktown\Projects\cc-discord-presence\.claude-plugin\plugin.json

<interfaces>
Plan 04 added these endpoints that the skill will call:
POST /preview -- accepts extended PreviewPayload with smallImage, smallText, largeText, buttons, startTimestamp, sessionCount, fakeSessions
GET /preview/messages?preset=X&amp;activity=Y&amp;count=N&amp;detail=Z -- returns Activity objects for message rotation
GET /presets -- returns available presets with active preset
GET /status -- returns current daemon status
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /dsrcode:demo skill with 4 modes (per D-09, D-12)</name>
  <files>C:\Users\ktown\Projects\cc-discord-presence\skills\demo\SKILL.md</files>
  <action>
IMPORTANT: All work is in `C:\Users\ktown\Projects\cc-discord-presence`.

Replace the entire content of `skills/demo/SKILL.md` with a 4-mode menu-based demo skill.

The skill YAML header must have: name=demo, description="Preview Discord presence for screenshots and demos", disable-model-invocation=true. Use standard YAML frontmatter delimiters at the top of the file.

The skill body should guide Claude through 4 modes (D-09):

MODE MENU:
After verifying daemon is running (curl -sf http://127.0.0.1:19460/health), present:

  Which demo mode?
  1) Quick Preview -- Single preset, one screenshot
  2) Preset Tour -- Walk through all 8 presets sequentially
  3) Multi-Session Preview -- Simulate multiple projects open
  4) Message Rotation -- See how messages rotate over time

MODE 1 (Quick Preview):
1. Fetch presets via GET /presets, show options
2. Ask which preset and displayDetail level
3. POST /preview with realistic defaults (D-10): preset=CHOSEN, displayDetail=LEVEL, duration=120, details="Working on my-saas-app", state="Opus 4.6 | 250K tokens | $2.50 | 2h 15m", smallImage="coding", smallText="Editing auth.ts", largeText="my-saas-app (feature/auth)", startTimestamp=UNIX_NOW_MINUS_2H
4. Tell user: "Preview active for 120 seconds. Switch to Discord and take your screenshot."
5. Ask: "Another preset, or done?"

MODE 2 (Preset Tour -- D-12 auto-cycle):
1. Fetch all presets via GET /presets
2. For each preset (loop):
   a. POST /preview with the preset and realistic data (same as Quick Preview)
   b. Tell user which preset is showing
   c. Ask: "Screenshot taken? (yes/skip/stop)"
   d. On "yes" or "skip": continue to next preset. On "stop": exit loop
3. After all 8: "All presets captured!"

The auto-cycle is skill-driven (D-12): Claude controls the loop, not a server timer.

MODE 3 (Multi-Session Preview):
1. Ask how many projects (2-5)
2. POST /preview with fakeSessions array. Example for 3 projects: preset="minimal", displayDetail="standard", duration=120, sessionCount=3, fakeSessions with entries for "my-saas-app" (Opus 4.6, 250K, $2.50, coding), "api-gateway" (Sonnet 4.6, 180K, $1.20, terminal), "docs-site" (Haiku 4.5, 50K, $0.30, reading)
3. Tell user: "Multi-session preview active. Discord should show 3 projects."
4. Ask: "Adjust projects, or done?"

MODE 4 (Message Rotation):
1. Ask which preset and activity (coding/terminal/searching/reading/thinking)
2. GET /preview/messages?preset=X&amp;activity=Y&amp;count=10&amp;detail=standard
3. Display the returned messages in a formatted list showing time windows
4. Ask: "Try another preset/activity combo, or done?"

General guidelines in the skill:
- All curl commands target http://127.0.0.1:19460
- startTimestamp calculated as current Unix epoch minus desired duration
- No "Preview Mode" text in any payload (D-11)
- Use realistic project names and data (D-10)
  </action>
  <verify>
    <automated>cd C:\Users\ktown\Projects\cc-discord-presence && test -f skills/demo/SKILL.md && wc -l skills/demo/SKILL.md</automated>
  </verify>
  <done>
    - SKILL.md rewritten with 4 modes (Quick Preview, Preset Tour, Multi-Session Preview, Message Rotation)
    - Each mode has step-by-step Claude instructions with exact curl commands
    - Auto-cycle is skill-driven (D-12): Claude loops with user confirmation per preset
    - Realistic demo data used in all modes (D-10)
    - No "Preview Mode" indicator in any payload (D-11)
    - Daemon health check at entry point
  </done>
</task>

<task type="auto">
  <name>Task 2: Version bump + CHANGELOG + README demo section (per D-18, D-19, D-22)</name>
  <files>
    C:\Users\ktown\Projects\cc-discord-presence\main.go
    C:\Users\ktown\Projects\cc-discord-presence\scripts\start.sh
    C:\Users\ktown\Projects\cc-discord-presence\.claude-plugin\plugin.json
    C:\Users\ktown\Projects\cc-discord-presence\CHANGELOG.md
    C:\Users\ktown\Projects\cc-discord-presence\README.md
  </files>
  <action>
IMPORTANT: All work is in `C:\Users\ktown\Projects\cc-discord-presence`.

VERSION BUMP (D-18):
1. main.go: Change Version = "3.0.0" to Version = "3.1.0"
2. scripts/start.sh: Change VERSION="v3.0.0" to VERSION="v3.1.0"
3. .claude-plugin/plugin.json: Change "version": "3.0.0" to "version": "3.1.0"

CHANGELOG.md (D-19):
Read the existing CHANGELOG.md first. The current file has entries for v1.0.0 through v1.0.3. Add entries for v2.0.0, v3.0.0, and v3.1.0 between [Unreleased] and [1.0.3].

v3.1.0 (2026-04-06): Fixed -- Session count bug (JSONL + HTTP/UUID same project no longer duplicate). Added -- SessionSource enum, source-aware upgrade chain, extended POST /preview, GET /preview/messages, 4-mode demo skill, JSONL deprecation warning. Changed -- Resolver uses unique project count for tier.

v3.0.0 (2026-04-03): Added -- 7 slash commands, HTTP hooks, HookPayload parsing, 4 displayDetail levels, 40 messages per preset, GET /presets + GET /status + POST /preview, session_id fallback, start.sh auto-update. Changed -- hooks.json command to HTTP.

v2.0.0 (2026-04-02): Added -- Complete Go rewrite, HTTP hook server, 8 presets with 200+ messages, multi-session support, config hot-reload, stale detection, StablePick, exponential backoff, 5-platform release. Changed -- Node.js to Go, PID-based tracking.

Update version comparison links at bottom of CHANGELOG.

README.md DEMO SECTION (D-22):
Read existing README.md. Find or create a "## Demo Mode" section. Add table of 4 modes (Quick Preview, Preset Tour, Multi-Session Preview, Message Rotation) with descriptions. Add Preview API section with 3 curl examples (single preset, multi-session, message rotation). Note preview expiry behavior. Keep rest of README unchanged.
  </action>
  <verify>
    <automated>cd C:\Users\ktown\Projects\cc-discord-presence && grep -q "3.1.0" main.go && grep -q "v3.1.0" scripts/start.sh && grep -q "3.1.0" .claude-plugin/plugin.json && grep -q "3.1.0" CHANGELOG.md && echo "ALL VERSION CHECKS PASS"</automated>
  </verify>
  <done>
    - main.go Version = "3.1.0"
    - scripts/start.sh VERSION="v3.1.0"
    - .claude-plugin/plugin.json version = "3.1.0"
    - CHANGELOG.md has v3.1.0 entry with Fixed/Added/Changed sections
    - CHANGELOG.md has retroactive v3.0.0 and v2.0.0 entries
    - README.md has demo section with 4 modes and API examples
    - Version comparison links updated at bottom of CHANGELOG
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete Phase 3</name>
  <what-built>
    Complete Phase 3: session dedup bug fix, resolver unique project counting, extended preview API, 4-mode demo skill, version 3.1.0 with changelog.
  </what-built>
  <how-to-verify>
    1. Build and run the daemon:
       cd C:\Users\ktown\Projects\cc-discord-presence
       go build -o cc-discord-presence.exe . then run cc-discord-presence.exe

    2. Verify session dedup fix:
       - Open ONE Claude Code window on a project
       - Check GET http://127.0.0.1:19460/sessions -- should show 1 session (not 2)
       - Verify the session has a "source" field with value "claude", "http", or "jsonl"

    3. Verify preview API:
       curl http://127.0.0.1:19460/preview/messages?preset=minimal&amp;activity=coding&amp;count=3
       Should return 3 JSON objects with details, state, smallImage fields.

    4. Test demo skill:
       - Run /dsrcode:demo in Claude Code
       - Verify 4-mode menu appears
       - Try Quick Preview: should set presence on Discord without "Preview Mode" text

    5. Verify version: cc-discord-presence.exe --version should output "3.1.0"

    6. Verify all tests pass: go test ./... -v -count=1
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. Skill instructions execute in Claude Code sandbox. CHANGELOG and README are documentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | I (Info Disclosure) | CHANGELOG | accept | Changelog is public documentation; no sensitive data |
| T-03-05-02 | T (Tampering) | Version strings | mitigate | Version checked in 3 files; verify command confirms consistency |
</threat_model>

<verification>
1. go test ./... -v -count=1 passes all tests (including 13 new Phase 3 tests)
2. go build ./... succeeds
3. Version "3.1.0" consistent across main.go, start.sh, plugin.json
4. CHANGELOG.md follows Keep a Changelog format with correct sections
5. Demo skill menu offers 4 modes with curl commands
6. README.md demo section documents all 4 modes and API
</verification>

<success_criteria>
Phase 3 complete: Session dedup bug fixed, unique project counting in resolver, extended preview API with fakeSessions and message rotation, 4-mode demo skill, version 3.1.0 with changelog and README updates. Human verification confirms Discord shows correct session count and preview works.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fix-discord-presence-session-count-and-enhance-demo-mode/03-05-SUMMARY.md`
</output>
