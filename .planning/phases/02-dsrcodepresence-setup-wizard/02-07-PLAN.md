---
phase: 02-dsrcodepresence-setup-wizard
plan: 07
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - commands/dsrcode:setup.md
  - commands/dsrcode:preset.md
  - commands/dsrcode:status.md
  - commands/dsrcode:log.md
  - commands/dsrcode:doctor.md
  - commands/dsrcode:update.md
  - commands/dsrcode:demo.md
autonomous: true
requirements:
  - DSR-01
  - DSR-02
  - DSR-03
  - DSR-04
  - DSR-05
  - DSR-06
  - DSR-07
  - DSR-30

must_haves:
  truths:
    - "/dsrcode:setup guides through 7 phases: prerequisites, discord test, base config, advanced, hook diagnosis, config write, verification"
    - "/dsrcode:preset shows current preset, lists all 8, lets user switch via POST /config"
    - "/dsrcode:status calls GET /status and displays daemon, discord, sessions, hooks info"
    - "/dsrcode:log reads last N lines from ~/.claude/discord-presence.log"
    - "/dsrcode:doctor runs 7-category diagnostics with OK/Warning/Error status icons"
    - "/dsrcode:update downloads latest binary from GitHub Releases"
    - "/dsrcode:demo uses POST /preview for screenshot mode"
    - "/dsrcode:status triggers self-identifying hook via Bash call per D-30"
  artifacts:
    - path: "commands/dsrcode:setup.md"
      provides: "7-phase guided setup wizard instructions"
      min_lines: 80
    - path: "commands/dsrcode:doctor.md"
      provides: "7-category diagnostic instructions"
      min_lines: 60
    - path: "commands/dsrcode:status.md"
      provides: "Status display instructions with self-triggering hook"
      contains: "GET /status"
    - path: "commands/dsrcode:preset.md"
      provides: "Preset quick-switch instructions"
      contains: "POST /config"
  key_links:
    - from: "commands/dsrcode:status.md"
      to: "server/server.go"
      via: "GET /status endpoint"
      pattern: "127\\.0\\.0\\.1:19460/status"
    - from: "commands/dsrcode:preset.md"
      to: "server/server.go"
      via: "GET /presets + POST /config endpoints"
      pattern: "127\\.0\\.0\\.1:19460/presets"
    - from: "commands/dsrcode:demo.md"
      to: "server/server.go"
      via: "POST /preview endpoint"
      pattern: "127\\.0\\.0\\.1:19460/preview"
---

<objective>
Create 7 slash-command .md files in the commands/ directory. Each file contains Claude-as-Wizard instructions that Claude reads and follows when the user types the command.

Purpose: Per D-01, slash commands use the Claude-as-Wizard pattern -- each .md file IS the prompt that instructs Claude to guide the user interactively. No Go TUI, no MCP server.
Output: 7 markdown files in commands/ directory, each with structured instructions for Claude to follow.
</objective>

<execution_context>
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-dsrcodepresence-setup-wizard/02-CONTEXT.md
@.planning/phases/02-dsrcodepresence-setup-wizard/02-RESEARCH.md
@.planning/phases/02-dsrcodepresence-setup-wizard/02-03-SUMMARY.md

Working directory for all tasks: C:\Users\ktown\Projects\cc-discord-presence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /dsrcode:setup, /dsrcode:preset, /dsrcode:status, /dsrcode:log commands</name>
  <files>commands/dsrcode:setup.md, commands/dsrcode:preset.md, commands/dsrcode:status.md, commands/dsrcode:log.md</files>
  <read_first>
    C:\Users\ktown\Projects\cc-discord-presence\.claude-plugin\plugin.json
    C:\Users\ktown\Projects\cc-discord-presence\config\config.go
    C:\Users\ktown\Projects\cc-discord-presence\server\server.go
  </read_first>
  <action>
1. Create commands/ directory: `mkdir -p commands`

2. Create commands/dsrcode:setup.md with this EXACT content:
   ```markdown
   # /dsrcode:setup -- Guided Setup Wizard

   You are guiding the user through DSR Code Presence setup. Follow these phases IN ORDER. Use Bash tool for checks and Read/Write for config files.

   ## Phase A: Prerequisites
   1. Check binary exists: `ls ~/.claude/bin/cc-discord-presence-* 2>/dev/null`
   2. If not found, inform user: "Binary not found. It will be downloaded automatically on next session start. Run `/clear` to trigger SessionStart hook."
   3. Check if daemon is running: `curl -sf http://127.0.0.1:19460/health 2>/dev/null`
   4. If not running, try to start: find binary and run it in background
   5. Check Discord desktop is running (platform-specific process check)

   ## Phase B: Discord Connection Test
   1. Call `curl -sf http://127.0.0.1:19460/health`
   2. If status is "ok", report "Discord daemon is running"
   3. Call `curl -sf http://127.0.0.1:19460/status` to check discordConnected
   4. If discordConnected is false, advise: "Make sure Discord desktop app is running and restart the daemon"

   ## Phase C: Base Configuration
   1. Show the 8 available presets by calling: `curl -sf http://127.0.0.1:19460/presets`
   2. Display each preset with name, description, and 3 sample messages
   3. Ask user to choose a preset (default: minimal)
   4. Ask user for Discord Application ID (show where to find it: Discord Developer Portal > Applications)
   5. If user doesn't have one, explain: "You need a Discord Application for custom icons. Use the default DSR Code app ID or create your own at https://discord.com/developers/applications"

   ## Phase D: Advanced Settings (Optional)
   Ask: "Would you like to configure advanced settings? (port, bind address, log level, timeouts, display detail, buttons)"
   If yes:
   1. Port (default 19460)
   2. Bind address (default 127.0.0.1)
   3. Log level (debug/info/warn/error, default info)
   4. Display detail (minimal/standard/verbose/private, default minimal) -- explain each level
   5. Idle timeout (default 10m)
   6. Buttons (up to 2 clickable links, label + URL)
   If no: skip to Phase E

   ## Phase E: Hook Diagnosis
   1. Read hooks/hooks.json from plugin root and verify all 4 hooks are HTTP type
   2. Check statusline is working: `curl -sf http://127.0.0.1:19460/health`
   3. Report hook status

   ## Phase F: Write Config + Restart Daemon
   1. Build config JSON from user choices
   2. Write to ~/.claude/discord-presence-config.json using Write tool
   3. The daemon will hot-reload the config automatically (fsnotify watches the config dir)
   4. Wait 2 seconds, then verify reload: `curl -sf http://127.0.0.1:19460/status`

   ## Phase G: Verification + Summary
   1. Call GET /status and display:
      - Version, uptime
      - Discord connected status
      - Active preset
      - Display detail level
      - Number of active sessions
   2. Show "Setup complete!" summary with chosen settings
   3. Suggest: "Try `/dsrcode:status` to see your current status, or `/dsrcode:demo` to preview how it looks on Discord"
   ```

3. Create commands/dsrcode:preset.md with this content:
   ```markdown
   # /dsrcode:preset -- Quick Preset Switch

   You help the user quickly switch between display presets.

   ## Steps

   1. Fetch current state: `curl -sf http://127.0.0.1:19460/presets`
   2. Display the current active preset (highlighted)
   3. List all 8 presets with name, description, and 3 sample coding messages
   4. Ask: "Which preset would you like to switch to?"
   5. When user picks one, send: `curl -sf -X POST -H "Content-Type: application/json" -d '{"preset": "CHOSEN"}' http://127.0.0.1:19460/config`
   6. Verify by fetching /presets again
   7. Show: "Switched to {preset}. Changes take effect immediately (hot-reload)."

   ## Error Handling
   - If daemon not running (curl fails): "Daemon is not running. Start a new Claude Code session or run the binary manually."
   - If preset name invalid: show available presets again
   ```

4. Create commands/dsrcode:status.md with this content:
   ```markdown
   # /dsrcode:status -- Status Display

   You display the current DSR Code Presence status.

   ## Self-Triggering Hook (D-30)
   Before fetching status, run a quick command to trigger the PreToolUse hook, which identifies this session:
   ```bash
   echo "dsrcode-status-trigger" > /dev/null
   ```
   This Bash call triggers the PreToolUse hook, which sends session_id to the server.

   ## Steps

   1. Trigger self-identifying hook (see above)
   2. Fetch status: `curl -sf http://127.0.0.1:19460/status`
   3. Parse and display:

   ### Daemon
   - Version: {version}
   - Uptime: {uptime}
   - Discord: {connected ? "Connected" : "Disconnected"}

   ### Config
   - Preset: {config.preset}
   - Display Detail: {config.displayDetail}
   - Port: {config.port}

   ### Sessions ({sessions.length} active)
   For each session:
   - Project: {projectName} ({branch})
   - Status: {status} (active/idle)
   - Model: {model}
   - Tokens: {totalTokens} | Cost: {totalCostUsd}
   - Last Activity: {lastActivityAt}
   - Last File: {lastFile}
   - Mark the current session (match by cwd + most recent lastActivityAt)

   ### Hook Statistics
   - Total hooks received: {hookStats.total}
   - By type: {hookStats.byType}
   - Last received: {hookStats.lastReceivedAt}

   ## Error Handling
   - If daemon not running: "Daemon is not running. It starts automatically with each Claude Code session. Run `/clear` to trigger a SessionStart."
   ```

5. Create commands/dsrcode:log.md with this content:
   ```markdown
   # /dsrcode:log -- Log Viewer

   You display recent log entries from the DSR Code Presence daemon.

   ## Steps

   1. Read the log file: `tail -50 ~/.claude/discord-presence.log 2>/dev/null`
   2. If file doesn't exist or is empty: "No log file found at ~/.claude/discord-presence.log. The daemon may not have started yet."
   3. Display the last 50 lines formatted for readability
   4. Ask: "Show more? (Enter a number for more lines, or 'all' for the full log)"
   5. If user wants more: `tail -N ~/.claude/discord-presence.log` or `cat ~/.claude/discord-presence.log`
   6. Highlight any ERROR or WARN level entries

   ## Tips
   - For real-time monitoring, suggest: set logLevel to "debug" via `/dsrcode:setup` advanced settings
   - To clear the log: `> ~/.claude/discord-presence.log`
   ```
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && test -f commands/dsrcode:setup.md && test -f commands/dsrcode:preset.md && test -f commands/dsrcode:status.md && test -f commands/dsrcode:log.md && echo "All 4 command files exist" && wc -l commands/dsrcode:setup.md | awk '{if($1>=80) print "setup.md OK ("$1" lines)"; else {print "setup.md TOO SHORT ("$1" lines)"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - commands/dsrcode:setup.md exists with >= 80 lines
    - commands/dsrcode:setup.md contains "Phase A" through "Phase G"
    - commands/dsrcode:setup.md contains "127.0.0.1:19460"
    - commands/dsrcode:preset.md exists and contains "POST" and "/config"
    - commands/dsrcode:preset.md contains "127.0.0.1:19460/presets"
    - commands/dsrcode:status.md exists and contains "GET" and "/status"
    - commands/dsrcode:status.md contains "dsrcode-status-trigger" (self-triggering hook per D-30)
    - commands/dsrcode:log.md exists and contains "discord-presence.log"
  </acceptance_criteria>
  <done>4 slash-command files created: setup (7-phase wizard), preset (quick-switch), status (with self-triggering hook per D-30), log (log viewer).</done>
</task>

<task type="auto">
  <name>Task 2: Create /dsrcode:doctor, /dsrcode:update, /dsrcode:demo commands</name>
  <files>commands/dsrcode:doctor.md, commands/dsrcode:update.md, commands/dsrcode:demo.md</files>
  <read_first>
    C:\Users\ktown\Projects\cc-discord-presence\commands\dsrcode:setup.md
    C:\Users\ktown\Projects\cc-discord-presence\server\server.go
    C:\Users\ktown\Projects\cc-discord-presence\scripts\start.sh
  </read_first>
  <action>
1. Create commands/dsrcode:doctor.md with this content (per D-10, flutter doctor style):
   ```markdown
   # /dsrcode:doctor -- Diagnostics

   You run a comprehensive diagnostic check of the DSR Code Presence installation, inspired by `flutter doctor`. Display results with status icons: [OK], [!] (warning), [X] (error).

   ## Category 1: Binary & Daemon
   1. Check binary exists: `ls ~/.claude/bin/cc-discord-presence-* 2>/dev/null`
      - Found: [OK] Binary found at {path}
      - Not found: [X] Binary not found. Run `/clear` to trigger auto-download.
   2. Check binary version: `~/.claude/bin/cc-discord-presence-* --version 2>/dev/null`
      - Matches v3.0.0: [OK] Version {version}
      - Mismatch: [!] Version {version}, expected v3.0.0. Run `/dsrcode:update`.
   3. Check daemon running: `curl -sf http://127.0.0.1:19460/health 2>/dev/null`
      - Running: [OK] Daemon running (uptime: {uptime})
      - Not running: [X] Daemon not running. Run `/clear` to restart.

   ## Category 2: Discord Desktop
   1. Check Discord process:
      - Windows: `tasklist | grep -i discord`
      - macOS: `pgrep -x Discord`
      - Linux: `pgrep -x discord`
   2. If running: [OK] Discord desktop is running
   3. If not: [X] Discord desktop not detected. Start Discord and restart the daemon.
   4. Check Discord IPC connection: from GET /status discordConnected field
      - Connected: [OK] Discord IPC connected
      - Not connected: [!] Discord IPC not connected. Try restarting both Discord and the daemon.

   ## Category 3: Hooks & Activity Tracking
   1. Read hooks/hooks.json from plugin root: verify 4 HTTP hooks exist
      - All HTTP: [OK] 4 HTTP hooks configured (PreToolUse, UserPromptSubmit, Stop, Notification)
      - Missing hooks: [!] Only {n} hooks found, expected 4
      - Command type detected: [X] Legacy command hooks detected. Hooks should be HTTP type.
   2. Check hook stats from GET /status: hookStats.total
      - total > 0: [OK] {total} hooks received (last: {lastReceivedAt})
      - total == 0: [!] No hooks received yet. Activity tracking may not be working.

   ## Category 4: Statusline & Token/Cost
   1. From GET /status sessions: check if any session has model, totalTokens, or totalCostUsd set
      - Has data: [OK] Statusline data flowing (model: {model}, tokens: {tokens})
      - No data: [!] No statusline data. This populates over time as you use Claude Code.

   ## Category 5: Config
   1. Check config file: `cat ~/.claude/discord-presence-config.json 2>/dev/null`
      - Exists: [OK] Config found. Preset: {preset}, Display: {displayDetail}
      - Missing: [!] No config file. Using defaults. Run `/dsrcode:setup` to configure.
   2. Validate JSON: try parsing the config file
      - Valid: [OK] Config is valid JSON
      - Invalid: [X] Config has JSON syntax errors. Run `/dsrcode:setup` to regenerate.

   ## Category 6: Sessions
   1. From GET /status sessions array:
      - Has sessions: [OK] {n} active session(s)
      - Empty: [!] No sessions registered. Hooks may not be firing.
   2. For each session, show: project, status, lastActivityAt

   ## Category 7: Platform
   1. Detect OS: `uname -s`
   2. Check required tools: curl, bash, jq (optional)
   3. Report: [OK] {OS} {arch}, curl available, bash available

   ## Summary
   Count: {ok} OK, {warnings} warnings, {errors} errors
   If errors > 0: "Run the suggested fixes above, then re-run `/dsrcode:doctor`"
   If warnings only: "Everything is functional. Warnings are informational."
   If all OK: "Everything looks good!"
   ```

2. Create commands/dsrcode:update.md:
   ```markdown
   # /dsrcode:update -- Binary Updater

   You help the user update the DSR Code Presence binary to the latest version.

   ## Steps

   1. Check current version: `~/.claude/bin/cc-discord-presence-* --version 2>/dev/null`
   2. Check latest release: `curl -sf https://api.github.com/repos/DSR-Labs/cc-discord-presence/releases/latest | jq -r '.tag_name'`
      - Alternative: `gh release view --repo DSR-Labs/cc-discord-presence --json tagName -q '.tagName' 2>/dev/null`
   3. Compare versions:
      - Same: "You're already on the latest version ({version})."
      - Different: "Update available: {current} -> {latest}"
   4. If update available, ask: "Update now? (y/n)"
   5. If yes:
      a. Stop daemon: `curl -sf -X POST http://127.0.0.1:19460/shutdown 2>/dev/null; sleep 1`
         Or kill by PID: `kill $(cat ~/.claude/discord-presence.pid) 2>/dev/null`
      b. Detect platform (uname -s, uname -m)
      c. Download new binary:
         ```bash
         BINARY_NAME="cc-discord-presence-{os}-{arch}"
         curl -fsSL "https://github.com/DSR-Labs/cc-discord-presence/releases/download/{version}/${BINARY_NAME}" -o ~/.claude/bin/${BINARY_NAME}
         chmod +x ~/.claude/bin/${BINARY_NAME}
         ```
      d. Verify: `~/.claude/bin/cc-discord-presence-* --version`
      e. Restart daemon: run the binary in background
      f. Report: "Updated to {version} and restarted."

   ## Error Handling
   - No internet: "Cannot reach GitHub. Check your connection."
   - No releases: "No releases found for DSR-Labs/cc-discord-presence. The first release may not have been published yet."
   - Download fails: "Download failed. Try manually: gh release download --repo DSR-Labs/cc-discord-presence"
   ```

3. Create commands/dsrcode:demo.md:
   ```markdown
   # /dsrcode:demo -- Preview Mode for Screenshots

   You help the user preview how DSR Code Presence looks on Discord for screenshot generation. Uses POST /preview to set a temporary presence.

   ## Steps

   1. Check daemon is running: `curl -sf http://127.0.0.1:19460/health 2>/dev/null`
      - Not running: "Daemon must be running. Run `/clear` to start it."

   2. Ask: "Which preset would you like to preview?"
      - Fetch presets: `curl -sf http://127.0.0.1:19460/presets`
      - Show the 8 options with sample messages

   3. Ask: "Which display detail level? (minimal/standard/verbose/private)"

   4. Set the preview via POST /preview:
      ```bash
      curl -sf -X POST -H "Content-Type: application/json" \
        -d '{"preset": "CHOSEN", "displayDetail": "LEVEL", "duration": 120, "details": "Working on my-project", "state": "Sonnet 4.6 | 150K tokens | $1.50 | 2h 15m"}' \
        http://127.0.0.1:19460/preview
      ```

   5. Show: "Preview is now active on Discord for 120 seconds. Switch to Discord and take your screenshot!"

   6. Ask: "Ready to try another preset, or done?"
      - Another: go back to step 2
      - Done: "Preview will auto-expire. Normal presence will resume."

   ## Tips
   - Screenshots should show the Discord profile popup with the Rich Presence
   - For README screenshots, use each preset once with "standard" display detail
   - The preview shows exactly what other users would see on your Discord profile
   ```
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && test -f commands/dsrcode:doctor.md && test -f commands/dsrcode:update.md && test -f commands/dsrcode:demo.md && echo "All 3 command files exist" && wc -l commands/dsrcode:doctor.md | awk '{if($1>=60) print "doctor.md OK ("$1" lines)"; else {print "doctor.md TOO SHORT ("$1" lines)"; exit 1}}' && ls commands/ | wc -l | awk '{if($1==7) print "Total: 7 command files OK"; else {print "Expected 7 files, got "$1; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - commands/dsrcode:doctor.md exists with >= 60 lines
    - commands/dsrcode:doctor.md contains "Category 1" through "Category 7"
    - commands/dsrcode:doctor.md contains "[OK]" and "[X]" and "[!]"
    - commands/dsrcode:update.md exists and contains "github.com/repos/DSR-Labs"
    - commands/dsrcode:update.md contains "--version"
    - commands/dsrcode:demo.md exists and contains "POST /preview" or "/preview"
    - commands/dsrcode:demo.md contains "127.0.0.1:19460/preview"
    - commands/ directory contains exactly 7 files
  </acceptance_criteria>
  <done>7 slash-command files created in commands/. doctor has 7-category diagnostics with status icons. update checks GitHub Releases for new versions. demo uses POST /preview for screenshot generation.</done>
</task>

</tasks>

<verification>
cd C:/Users/ktown/Projects/cc-discord-presence && ls -la commands/ && wc -l commands/*.md
</verification>

<success_criteria>
- 7 command files exist in commands/ directory
- /dsrcode:setup has 7 phases (A-G) covering full guided setup
- /dsrcode:preset uses GET /presets and POST /config
- /dsrcode:status uses GET /status with self-triggering hook
- /dsrcode:log reads discord-presence.log
- /dsrcode:doctor has 7 diagnostic categories with OK/Warning/Error icons
- /dsrcode:update checks GitHub Releases for updates
- /dsrcode:demo uses POST /preview for screenshot mode
- All commands reference correct localhost:19460 endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/02-dsrcodepresence-setup-wizard/02-07-SUMMARY.md`
</output>
