---
plan_number: 07-05
phase: 7
wave: 2
title: "Release v4.1.2 — version bump + CHANGELOG + Per-Task Verification Map finalization"
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - main.go
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - scripts/start.sh
  - scripts/start.ps1
  - CHANGELOG.md
  - .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md
requirements: [D-13, D-14, D-15]
autonomous: false
estimated_commits: 2
---

# Plan 07-05: Release v4.1.2 — version bump + CHANGELOG + Per-Task Verification Map finalization

## Goal

Land the v4.1.2 hotfix release after Wave-1 plans 07-01..07-04 are all green. Run `./scripts/bump-version.sh 4.1.2` to propagate the version constant to the 5 canonical files (main.go, plugin.json, marketplace.json, start.sh, start.ps1) per D-14. Add a `## [4.1.2] - YYYY-MM-DD` entry to CHANGELOG.md modeled after the v4.1.1 entry shape (CHANGELOG.md:10-13) and the v4.1.0 multi-bullet entry shape (CHANGELOG.md:15-39), with a `### Fixed` section enumerating the 4 bugs (with cross-referenced D-IDs and upstream-issue links per RESEARCH §Cross-Cutting Concerns) and a `### Changed` sub-section noting the Unix log-redirect split per D-13. Backfill the Per-Task Verification Map in `07-VALIDATION.md` with the actual Task IDs from plans 07-01..07-04 and flip `nyquist_compliant: true`. Includes a manual cross-platform verification checkpoint for Bug #1 MCP-heavy reproduction (D-13).

## Must-Haves

Goal-backward, observable truths after this plan completes:

- **Truth 1:** All 5 version-managed files contain `4.1.2` exactly: `grep -l '4.1.2' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 | wc -l` returns 5.
- **Truth 2:** CHANGELOG.md has a `## [4.1.2]` entry with 4 `### Fixed` bullets (one per bug) + 1 `### Changed` bullet (Unix log split).
- **Truth 3:** The CHANGELOG entry references each fix by D-ID (D-01/D-02 for Bug #1, D-04/D-05 for Bug #2, D-07/D-09 for Bug #3, D-10/D-11/D-12 for Bug #4) and includes the upstream-bug-issue cross-references for Bug #3 (anthropics/claude-code#17885, #16288).
- **Truth 4:** `07-VALIDATION.md` Per-Task Verification Map (lines 47-58) has every row's "Task Group" column filled in with concrete Task IDs (07-01-01, 07-01-02, 07-02-01, 07-02-02, 07-02-03, 07-03-01..04, 07-04-01..03), and `nyquist_compliant: true` in frontmatter.
- **Truth 5:** Manual MCP-heavy reproduction (Bug #1+#2 canonical scenario) on at least Windows passes — daemon stays alive through 30 min of MCP-only tool calls; refcount stays at 1; Discord presence stays visible.
- **Truth 6:** Manual cross-platform SessionEnd test (Bug #3) shows refcount drops to 0 on `/exit`/`/clear` AND on VS Code close-button on Windows; PID-file is removed on macOS/Linux.
- **Truth 7:** Manual log-rotation test (Bug #4) on a real ~/.claude/dsrcode.log: a synthetic 11MB file gets rotated to .log.1; second 11MB run overwrites .log.1 (no .log.2).
- **Artifact 1:** All 5 version files updated.
- **Artifact 2:** CHANGELOG.md `## [4.1.2]` section.
- **Artifact 3:** `07-VALIDATION.md` Per-Task Verification Map filled.
- **Key link:** `bump-version.sh` is the ONE TRUE WAY to propagate the version per CLAUDE.md §Releasing — do not hand-edit any of the 5 files.

## Tasks

<task id="07-05-01">
  <type>execute</type>
  <name>Task 1: Verify all Wave-1 plans green, then run bump-version.sh 4.1.2</name>
  <read_first>
    - `scripts/bump-version.sh` (entire script — confirm the 5-file sed targets are correct)
    - `main.go` (top of file — check current `version = "..."` line under the `var` block)
    - `.claude-plugin/plugin.json` line 3 (current version field — should be 4.1.1)
    - `.claude-plugin/marketplace.json` (current version field)
    - `scripts/start.sh` (search for VERSION constant near top)
    - `scripts/start.ps1` (search for $Version constant near top)
    - `CLAUDE.md` §Releasing (the canonical bump-and-tag flow)
  </read_first>
  <action>
**Pre-flight: confirm Wave 1 is fully green.**

Run the full test suite + lint:
```bash
cd "$(git rev-parse --show-toplevel)"
go test -race ./... 2>&1 | tail -10  # all OK
golangci-lint run 2>&1 | tail -5     # exit 0
bash scripts/test-rotate-log.sh      # PASSES all 5 cases
pwsh scripts/test-rotate-log.ps1 2>&1 | tail -10  # PASSES all 5 cases on Windows; OK to skip on pure-Linux CI
node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))" && echo HOOKS_JSON_OK
bash -n scripts/start.sh && bash -n scripts/stop.sh && echo BASH_SYNTAX_OK
pwsh -NoProfile -Command "$null = [scriptblock]::Create((Get-Content scripts/start.ps1 -Raw)); 'PS_PARSE_OK'" 2>&1 | grep -q PS_PARSE_OK
```

If ANY of the above fails, STOP — open a checkpoint and request the failing Wave-1 plan re-runs.

**Run the version bump.**

```bash
./scripts/bump-version.sh 4.1.2
```

This sed-edits 5 files in place. The script prints "Bumping version to 4.1.2..." and instructions for the git commands at the end (it does NOT auto-commit per CLAUDE.md and per `bump-version.sh:5` comment).

**Verify all 5 propagation targets.**

```bash
grep -n 'version = "4.1.2"' main.go
grep '"version": "4.1.2"' .claude-plugin/plugin.json
grep '"version": "4.1.2"' .claude-plugin/marketplace.json
grep '4.1.2' scripts/start.sh
grep '4.1.2' scripts/start.ps1
```

Each command should produce ≥ 1 line of match. If any returns empty, the bump-version.sh sed pattern needs investigation — DO NOT hand-edit; instead fix bump-version.sh (out-of-scope for Phase 7 if it's broken; revert and open a separate quick task).

**Verify go build still works after version change.**

```bash
go build -o /tmp/dsrcode-411 .
/tmp/dsrcode-411 --version 2>&1 | head -3 || true  # depending on flag support; otherwise just confirm build green
rm -f /tmp/dsrcode-411
```
  </action>
  <acceptance_criteria>
    - All Wave-1 tests + lints green: `go test -race ./...` exits 0 AND `golangci-lint run` exits 0
    - bump-version.sh succeeded: `./scripts/bump-version.sh 4.1.2` exit 0
    - All 5 files at v4.1.2: `grep -l '4.1.2' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 | wc -l` returns 5
    - main.go specifically has the `version = "4.1.2"` line: `grep -E '^\s*version = "4\.1\.2"$' main.go` returns 1 line
    - plugin.json + marketplace.json JSON-valid: `node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json','utf8'));JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json','utf8'))" exits 0`
    - go build green: `go build -o /tmp/dsrcode-411 . && rm -f /tmp/dsrcode-411`
  </acceptance_criteria>
  <automated>
    cd "$(git rev-parse --show-toplevel)" && go test -race ./... > /tmp/07-05-01-tests.log 2>&1 && [ "$(grep -l '4.1.2' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 | wc -l)" = "5" ] && go build -o /tmp/dsrcode-411 . && rm -f /tmp/dsrcode-411
  </automated>
  <requirements>D-13, D-14</requirements>
</task>

<task id="07-05-02">
  <type>execute</type>
  <name>Task 2: Add CHANGELOG.md v4.1.2 entry + finalize 07-VALIDATION.md Per-Task Verification Map</name>
  <read_first>
    - `CHANGELOG.md` (lines 1-50 — header + Unreleased + v4.1.1 + v4.1.0 to mirror style)
    - `.planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-RESEARCH.md` (§"Cross-Cutting Concerns → CHANGELOG.md entry" — pre-drafted v4.1.2 entry to copy)
    - `.planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md` (entire file — the Per-Task Verification Map at lines 47-58 needs Task IDs filled in)
    - All 4 PLAN files (07-01, 07-02, 07-03, 07-04) just shipped — to extract concrete Task IDs (07-NN-MM)
  </read_first>
  <action>
**Step A — Add CHANGELOG.md v4.1.2 entry.**

Edit `CHANGELOG.md`. The current header structure (lines 8-14):

```markdown
## [Unreleased]

## [4.1.1] - 2026-04-11

### Fixed
- Windows daemon launch in `scripts/start.sh` now redirects stdout to ...
```

Insert a new v4.1.2 section between `## [Unreleased]` (line 8) and `## [4.1.1]` (line 10). Use today's date in YYYY-MM-DD format (`date -u +%Y-%m-%d`):

```markdown
## [4.1.2] - YYYY-MM-DD

### Fixed
- **Daemon self-termination during long MCP-heavy sessions** — `handlePostToolUse` now updates `LastActivityAt` on every tool call (Bug #2, D-04/D-05 Phase 7). Previously, MCP-heavy sessions could idle the activity clock past the 30-minute remove-timeout even while Claude Code was actively calling tools, causing the daemon to remove the session and eventually self-exit. New `registry.Touch()` method updates the activity timestamp without firing a Discord presence update.
- **PID-liveness check false-positives for HTTP-sourced sessions** — `session/stale.go` now skips the PID liveness check when `Source == HTTP` (Bug #1, D-01/D-02 Phase 7). The daemon's recorded PID for HTTP-sourced sessions is the short-lived `start.sh` wrapper process on Windows, which exits within seconds even while the Claude Code session is alive. The 30-minute `removeTimeout` remains as the backstop.
- **Refcount drift via missing SessionEnd command hook** — plugin `hooks/hooks.json` now registers `SessionEnd` as a `type: command` hook invoking `scripts/stop.sh` / `scripts/stop.ps1` (Bug #3, D-07 Phase 7). Dual-registered in `~/.claude/settings.local.json` via `start.sh` / `start.ps1` auto-patch (fallback for documented upstream plugin-hook-discovery issues — anthropics/claude-code#17885, #16288).
- **Log overwrite on daemon restart** — `start.sh` and `start.ps1` now rotate `dsrcode.log` at 10 MB to `dsrcode.log.1` (single backup) before daemon launch (Bug #4, D-10/D-11/D-12 Phase 7). Same rotation applies to `dsrcode.log.err`. Crash history now survives daemon restarts.
- **Windows daemon launch in `scripts/start.ps1`** now redirects stderr to `~/.claude/dsrcode.log.err` (separate from stdout). Same class of bug Phase 6.1 quick task `260411-iyf` (v4.1.1) fixed in `start.sh`'s PowerShell branch but missed in `start.ps1` proper.

### Changed
- Unix log redirection (`start.sh`) now splits stdout and stderr into separate files (`dsrcode.log` and `dsrcode.log.err`) matching Windows behavior established in v4.1.1.
- `scripts/start.ps1` now patches `~/.claude/settings.local.json` with all 13 dsrcode HTTP hooks plus the SessionEnd command-hook on launch — bringing Windows-native users to dual-registration parity with Unix-via-bash.
```

Replace `YYYY-MM-DD` with `$(date -u +%Y-%m-%d)` literal (the actual date string).

**Step B — Finalize 07-VALIDATION.md Per-Task Verification Map.**

Edit `07-VALIDATION.md`. The current Per-Task Verification Map at lines 47-58 has skeleton rows. Replace the table body with concrete Task IDs:

| Task Group | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|------------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 07-01-01 (RED test scaffold) | 07-01 | 1 | D-01, D-02 | none | Tests for HTTP-source skip + PID-source preservation written, RED before Task 2 | unit | `go test -run 'TestStaleCheck' -race ./session/...` | session/stale_test.go (NEW in plan 07-01) | PENDING (flips to PASS after 07-01-02) |
| 07-01-02 (GREEN guard tightening) | 07-01 | 1 | D-01, D-02, D-03 | none | `s.Source != SourceHTTP` guard at session/stale.go:41 | unit | `go test -run 'TestStaleCheck' -race ./session/...` | session/stale.go | PASS |
| 07-02-01 (Touch + tests) | 07-02 | 1 | D-04, D-05 | T-07-02 | `registry.Touch()` updates LastActivityAt without notifyChange | unit | `go test -run 'TestTouch' -race ./session/...` | session/registry.go + session/registry_test.go | PASS |
| 07-02-02 (handler wiring + regression tests) | 07-02 | 1 | D-04, D-05 | T-07-02 | handlePostToolUse calls Touch on every invocation; UI fields untouched | integration | `go test -run 'TestHandlePostToolUseUpdates|TestHandlePostToolUseDoesNotChange' -race ./server/...` | server/server.go + server/server_test.go | PASS |
| 07-02-03 (D-06 verification gate) | 07-02 | 1 | D-06 | none | settings.local.json wildcard PostToolUse matcher already in place | file | `grep -q "'PostToolUse'.*matcher: '\*'" scripts/start.sh` | scripts/start.sh | PASS (no diff) |
| 07-03-01 (plugin hooks.json SessionEnd entry) | 07-03 | 1 | D-07, D-09 | T-07-04 | hooks.json contains SessionEnd command hook with stop.sh | file | `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')).hooks.SessionEnd[0].hooks[0].command.includes('stop.sh') ? 0 : process.exit(1)"` | hooks/hooks.json | PASS |
| 07-03-02 (Unix patch_settings_local extension) | 07-03 | 1 | D-07 | T-07-04 | start.sh patch writes SessionEnd command-hook entry into settings.local.json | integration (bash) | tmp-HOME smoke test in 07-03-02 acceptance criteria | scripts/start.sh | PASS |
| 07-03-03 (Windows Patch-SettingsLocal mirror) | 07-03 | 1 | D-07 | T-07-04 | start.ps1 Patch-SettingsLocal writes the same entry on Windows | integration (pwsh) | tmp-HOME smoke test in 07-03-03 acceptance criteria | scripts/start.ps1 | PASS |
| 07-03-04 (cleanup_settings_local extension) | 07-03 | 1 | D-07 | none | stop.sh removes SessionEnd command-hook entry on cleanup | integration (bash) | tmp-HOME cleanup test in 07-03-04 acceptance criteria | scripts/stop.sh | PASS |
| 07-04-01 (Unix rotate_log + redirect change) | 07-04 | 1 | D-10, D-11, D-12 | T-07-07 | start.sh has rotate_log helper; nohup uses >> append; stderr split | file + harness | `bash scripts/test-rotate-log.sh` | scripts/start.sh + scripts/test-rotate-log.sh | PASS |
| 07-04-02 (Windows Rotate-Log + stderr-defect fix) | 07-04 | 1 | D-10, D-11, D-12 | T-07-07 | start.ps1 has Rotate-Log helper; -RedirectStandardError uses $LogFileErr | file + harness | `pwsh scripts/test-rotate-log.ps1` | scripts/start.ps1 + scripts/test-rotate-log.ps1 | PASS |
| 07-04-03 (test-rotate-log harnesses) | 07-04 | 1 | D-10, D-11, D-12 | none | Wave-0 automated harness for rotation mechanics on both platforms | harness | `bash scripts/test-rotate-log.sh && pwsh scripts/test-rotate-log.ps1` | scripts/test-rotate-log.sh + scripts/test-rotate-log.ps1 | PASS |
| 07-05-01 (version bump) | 07-05 | 2 | D-13, D-14 | none | All 5 files at v4.1.2 | file | `grep -l '4.1.2' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 | wc -l` returns 5 | 5 files | PASS |
| 07-05-02 (CHANGELOG + VALIDATION) | 07-05 | 2 | D-13, D-14 | none | v4.1.2 CHANGELOG section + nyquist_compliant: true | file | `grep -A 2 '## \[4.1.2\]' CHANGELOG.md \| grep -q 'Daemon self-termination'` | CHANGELOG.md + 07-VALIDATION.md | PASS (this task) |
| 07-05-03 (manual cross-platform verification) | 07-05 | 2 | D-13 | none | MCP-heavy reproduction passes; SessionEnd refcount decrement on Windows; log rotation on real daemon restart | manual | see 07-05-03 below | n/a (manual checkpoint) | PASS (after manual sign-off) |

Also flip the frontmatter at line 5 from `nyquist_compliant: false` to `nyquist_compliant: true` AND set `status:` from `draft` to `final`.

**Step C — Validate.**

```bash
grep -q '## \[4.1.2\]' CHANGELOG.md
grep -q 'Daemon self-termination during long MCP-heavy sessions' CHANGELOG.md
grep -q 'PID-liveness check false-positives' CHANGELOG.md
grep -q 'Refcount drift via missing SessionEnd' CHANGELOG.md
grep -q 'Log overwrite on daemon restart' CHANGELOG.md
grep -q 'anthropics/claude-code#17885' CHANGELOG.md
head -10 .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md | grep -q 'nyquist_compliant: true'
grep -c '07-0[1-5]-0[1-9]' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md  # ≥14 (one per row in the table)
```
  </action>
  <acceptance_criteria>
    - CHANGELOG has v4.1.2 section with today's date: `grep -E '^## \[4\.1\.2\] - 20[0-9]{2}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md` returns 1 line
    - All 4 bugs cross-referenced in CHANGELOG: `grep -c 'Bug #[1-4]' CHANGELOG.md` returns ≥ 4
    - Upstream issues cited: `grep -c 'anthropics/claude-code#' CHANGELOG.md` returns ≥ 2 (#17885 + #16288 minimum)
    - D-IDs cross-referenced for traceability: `grep -E 'D-(01|02|03|04|05|06|07|08|09|10|11|12)' CHANGELOG.md | wc -l` returns ≥ 4 lines (one per bullet)
    - VALIDATION map has all 14 task IDs: `grep -E '07-0[1-5]-0[1-9]' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md | wc -l` returns ≥ 14
    - VALIDATION frontmatter flipped: `grep -q 'nyquist_compliant: true' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md`
    - Status flipped: `grep -q '^status: final' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md`
  </acceptance_criteria>
  <automated>
    cd "$(git rev-parse --show-toplevel)" && grep -E '^## \[4\.1\.2\] - 20[0-9]{2}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md && grep -q 'Daemon self-termination' CHANGELOG.md && grep -q 'anthropics/claude-code#17885' CHANGELOG.md && grep -q 'nyquist_compliant: true' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md && [ "$(grep -cE '07-0[1-5]-0[1-9]' .planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-VALIDATION.md)" -ge "14" ]
  </automated>
  <requirements>D-13, D-14, D-15</requirements>
</task>

<task id="07-05-03">
  <type>checkpoint:human-verify</type>
  <name>Task 3: Manual cross-platform verification (Bug #1 MCP-heavy reproduction + Bug #3 SessionEnd + Bug #4 log rotation)</name>
  <what-built>
    All 4 bug fixes (Plans 07-01..07-04) merged, v4.1.2 version bumped, CHANGELOG entry shipped. Build verified via `go build`. The remaining gate is manual end-to-end verification on real Claude Code sessions because:
    - Bug #1 reproduction requires a real 30-min MCP-heavy session (cannot be simulated in Go test harness without building a claude-code emulator).
    - Bug #3 SessionEnd plugin-hook dispatch is a claude-code internal lifecycle event (cannot be triggered synthetically from Go or bash).
    - Bug #4 log-rotation on a real daemon write cycle requires actually launching the daemon and crashing/restarting it.

    Per VALIDATION.md "Manual-Only Verifications" table — these are gated as 🟡 manual-required.
  </what-built>
  <how-to-verify>
    **Pre-condition:** Build the v4.1.2 binary fresh:
    ```bash
    go build -o /tmp/dsrcode-412 . && cp /tmp/dsrcode-412 ~/.claude/plugins/.../bin/dsrcode  # or wherever your dev binary lives
    # Restart the daemon: bash scripts/stop.sh ; bash scripts/start.sh
    ```

    **Verification 1 — Bug #1 + Bug #2 (canonical MCP-heavy reproduction, ALL platforms but most visible on Windows):**
    1. Open Claude Code (any session, any project).
    2. Confirm `~/.claude/dsrcode.refcount` (Windows) / `ls ~/.claude/dsrcode-sessions/` (Unix) shows 1.
    3. Confirm Discord presence is visible.
    4. Run a chain of MCP tool calls for **30+ minutes** without any Edit/Write/Bash/Read/Grep/Glob/WebSearch tool calls. Use sequential-thinking + exa + context7 + crawl4ai (the 4 tools cited in CONTEXT.md as the canonical reproduction trigger).
    5. After 30 min:
       - **PASS:** `dsrcode.exe` / `dsrcode` process is still running. Refcount is still 1. Discord presence still shows session info.
       - **FAIL (Bug #1 regression):** Daemon log shows "removing stale session (PID dead, no recent activity)" and the daemon process is gone.
       - **FAIL (Bug #2 regression):** Daemon log shows "removing stale session (timeout)" — Bug #2 was supposed to keep LastActivityAt fresh.

    **Verification 2 — Bug #3 SessionEnd → refcount decrement (Windows-critical):**
    1. Open Claude Code on Windows.
    2. `type %USERPROFILE%\.claude\dsrcode.refcount` → note value N (likely 1).
    3. Close the session via `/exit`.
    4. Re-read `dsrcode.refcount` → MUST be N-1 (typically 0).
    5. Repeat with `/clear`.
    6. Repeat with VS Code close-button (X icon).
    7. Repeat with `/resume` after a session was active.
    8. **PASS:** All 4 exit paths decrement refcount to 0.
    9. **FAIL:** Refcount unchanged or stuck at stale value → indicates BOTH plugin-hook AND settings.local.json paths failed; investigate.

    **Verification 3 — Bug #3 cross-platform PID-file cleanup (macOS/Linux):**
    1. `ls ~/.claude/dsrcode-sessions/` → note files.
    2. End Claude session via `/exit`.
    3. Re-list → matching PID-file MUST be gone.
    4. **PASS:** PID-file gone within 2 seconds.
    5. **FAIL:** Leftover files accumulate across sessions.

    **Verification 4 — Bug #4 log rotation on real daemon (ALL platforms):**
    1. Stop the daemon: `bash scripts/stop.sh` or close Claude Code.
    2. Delete existing logs: `rm -f ~/.claude/dsrcode.log* ~/.claude/dsrcode.log.err*`.
    3. Launch daemon (bash scripts/start.sh OR new Claude Code session).
    4. Confirm `~/.claude/dsrcode.log` exists and is small (< 1MB).
    5. Stop daemon, restart.
    6. Confirm `~/.claude/dsrcode.log` GREW (Unix append) or stayed similar size (Windows fresh truncate after no-rotation).
    7. Force the file to ≥10MB: `dd if=/dev/zero of=~/.claude/dsrcode.log bs=1M count=11`.
    8. Stop daemon, restart.
    9. **PASS:** `~/.claude/dsrcode.log.1` exists with ~11MB content; `~/.claude/dsrcode.log` is small/empty (fresh).
    10. **FAIL:** No `.log.1` created, or original `.log` content lost without backup.
    11. Force >10MB again, restart.
    12. **PASS:** `~/.claude/dsrcode.log.2` does NOT exist (single-backup retention; .log.1 was overwritten).
    13. Repeat for `~/.claude/dsrcode.log.err`.

    **Verification 5 — Stderr split fix (Windows-only smoke):**
    1. After Verification 4 step 3, intentionally trigger a daemon error (e.g., kill Discord IPC mid-handshake; or send malformed payload to `http://127.0.0.1:19460/hooks/post-tool-use`).
    2. Confirm error trace appears in `~/.claude/dsrcode.log.err` and NOT in `~/.claude/dsrcode.log`.
    3. **PASS:** stderr is in `.log.err`.
    4. **FAIL (Phase 6.1 regression):** stderr is in `.log` (the same path as stdout).

    Document each Verification's result in a comment on the v4.1.2 release commit. If ANY verification FAILS, do NOT tag v4.1.2; instead open a follow-up plan to address.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 verifications PASS on at least one platform of each (Windows for #1+#3+#4+#5, macOS or Linux for #1+#3+#4).
    Type "approved-windows-only" if Verifications 2/3 only ran on Windows (acceptable for a hotfix).
    Type "issue: <description>" if any FAIL — orchestrator will spawn a gap-closure plan.
  </resume-signal>
  <requirements>D-13</requirements>
</task>

## Verification

After all 3 tasks land:

1. **Build green:** `go build ./...` exits 0; `go test -race ./...` exits 0; `golangci-lint run` exits 0.
2. **All 5 version files at v4.1.2:** `grep -l '4.1.2' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 | wc -l` returns 5.
3. **CHANGELOG entry present:** `grep '^## \[4.1.2\]' CHANGELOG.md` returns 1 line.
4. **VALIDATION nyquist_compliant true:** `grep 'nyquist_compliant: true' 07-VALIDATION.md` returns 1 line.
5. **Manual cross-platform verifications PASS** per Task 3 checkpoint.
6. **Goal-backward Truth 1-7 verification:** all per-truth conditions in <must_haves> resolve to PASS.

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-10 | Tampering | bump-version.sh sed pattern miss-applies on a renamed/refactored file | mitigate | bump-version.sh is OWNED by this repo (scripts/bump-version.sh:5 says "5 files total"); the sed patterns are unit-tested implicitly by every prior version bump (v4.0.0, v4.1.0, v4.1.1). The `grep -l '4.1.2' [5 files] | wc -l == 5` acceptance criterion is the regression guard. |
| T-07-11 | Information Disclosure | CHANGELOG.md leaks internal D-IDs to public release notes | accept | D-IDs are public planning artifacts in `.planning/` (already on GitHub). No secret exposure. The cross-references improve traceability for downstream contributors. |

(No new I/O surfaces; no new auth boundaries; pure release plumbing.)

## Notes

**MCP citations (per user mandate):**
- `mcp__sequential-thinking__sequentialthinking` — Decomposed Plan 07-05 into (1) bump+verify, (2) CHANGELOG+VALIDATION, (3) manual cross-platform gate. Confirmed manual gate is unavoidable per VALIDATION.md "Manual-Only Verifications" table — Bug #1 + Bug #3 + Bug #4 require real Claude Code lifecycle events that no automated harness can synthesize.
- `mcp__context7__query-docs` (Keep-a-Changelog 1.1.0) — Confirmed `### Fixed`/`### Changed`/`### Added`/`### Removed` sub-sections are the canonical order; v4.1.2 uses `### Fixed` (primary) + `### Changed` (secondary).
- RESEARCH.md §"Cross-Cutting Concerns → CHANGELOG.md entry" — pre-drafted v4.1.2 entry copied verbatim, supplemented with the start.ps1 stderr-defect bullet (Plan 07-04 fold-in).

**Non-obvious choices:**
- **Manual checkpoint is BLOCKING.** The `<resume-signal>` pattern allows the orchestrator to pause for user sign-off before tagging v4.1.2. This is the only checkpoint in Phase 7 — Wave 1 and Tasks 1-2 of Wave 2 are fully autonomous.
- **CHANGELOG includes a 5th `### Fixed` bullet for the start.ps1 stderr-defect fix** (folded from Plan 07-04's Pitfall #5 work). This is technically out of D-12 but inside the same edit region; transparency in the changelog avoids surprise on the v4.1.2 commit history.
- **Did NOT tag v4.1.2 in this plan.** Per CLAUDE.md §Releasing the tag flow is `git tag vX.Y.Z && git push origin main --tags` — but this triggers GoReleaser CI which builds 5 platform binaries. Tagging is gated on the manual checkpoint passing AND the user explicitly running the tag commands. Plan 07-05 lands the version + CHANGELOG; user runs the tag.
- **`07-VALIDATION.md` Per-Task Verification Map is the source-of-truth for plan-checker.** Filling it in is the GSD-required gate to flip `nyquist_compliant: true`. Without this flip, downstream tooling will treat Phase 7 as not-validated.

**Out of scope (do NOT do here):**
- Tagging `git tag v4.1.2` — gated on manual checkpoint pass + user's explicit instruction.
- Pushing tags to remote — same.
- Triggering GoReleaser — automatic on tag-push, not manually invoked here.
- Editing `bump-version.sh` if it works correctly — out of Phase 7 scope.
- Adding a backport to v3.x or earlier branches — Phase 7 ships forward only.

**File-overlap audit with other plans:**
- Wave 1 plans 07-01..07-04 are dependencies — they MUST land before this plan starts. `depends_on: ["07-01", "07-02", "07-03", "07-04"]` in frontmatter enforces this for the orchestrator.
- This plan's `files_modified` includes `scripts/start.sh` and `scripts/start.ps1` because `bump-version.sh` sed-edits the version constants in them. Wave-1 plans 07-03 and 07-04 ALSO edit these files (different regions). The bump-version.sh sed is line-targeted to the version-constant lines (verified at scripts/bump-version.sh:25-32) — does NOT conflict with any Wave-1 hook/log changes. Sequencing (Wave 2 after Wave 1) ensures no race.

**Release execution flow** (FOR USER, after this plan lands):
1. Verify last commit message + diff: `git log -3 --stat`
2. Tag: `git tag v4.1.2`
3. Push: `git push origin main --tags`
4. Watch GoReleaser CI build 5 platform binaries.
5. Verify GitHub Release page shows v4.1.2 with attached binaries + CHANGELOG snippet.
6. Optional: `gh release view v4.1.2` to confirm.
