---
phase: 06-hook-system-overhaul
plan: 05
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified:
  - server/server.go
  - main.go
  - CHANGELOG.md
autonomous: false
requirements: [D-04, D-07, D-08, D-09, D-10, D-16]
must_haves:
  truths:
    - "Full build compiles and all tests pass"
    - "SessionEnd handler wired to auto-exit refcount check"
    - "syncAnalyticsToRegistry called from PostToolUse and SessionEnd paths"
    - "CHANGELOG.md documents v4.1.0 changes"
  artifacts:
    - path: "main.go"
      provides: "Fully wired main with auto-exit, no JSONL, analytics sync"
    - path: "CHANGELOG.md"
      provides: "v4.1.0 changelog entry"
  key_links:
    - from: "server/server.go"
      to: "main.go"
      via: "syncAnalyticsToRegistry callback"
      pattern: "syncAnalyticsToRegistry"
---

<objective>
Wire the analytics sync bridge between server hooks and the registry, ensure auto-exit triggers from both SessionEnd and stale detection, add CHANGELOG entry, and verify the complete integration.

Purpose: Plans 01-04 built the components. This plan wires them together and verifies the full system works end-to-end.
Output: Fully integrated main.go + server.go, CHANGELOG.md, verification checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-hook-system-overhaul-sessionend-posttooluse-precompact-hooks/06-CONTEXT.md
@.planning/phases/06-hook-system-overhaul-sessionend-posttooluse-precompact-hooks/06-01-SUMMARY.md
@.planning/phases/06-hook-system-overhaul-sessionend-posttooluse-precompact-hooks/06-03-SUMMARY.md
@.planning/phases/06-hook-system-overhaul-sessionend-posttooluse-precompact-hooks/06-04-SUMMARY.md

@main.go
@server/server.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire analytics sync callback and finalize integration</name>
  <files>server/server.go, main.go</files>
  <read_first>main.go (current state after Plan 04 — syncAnalyticsToRegistry function), server/server.go (current state after Plan 03 — handlePostToolUse and handleSessionEnd handlers)</read_first>
  <action>
1. Add an `onAnalyticsSync` callback to server.Server struct:
```go
onAnalyticsSync func(sessionID string) // called after token updates to sync analytics to registry
```

Add this to NewServer parameters and store it. If nil, skip calls.

2. In handlePostToolUse, after the throttled ParseTranscript goroutine updates tokens, call:
```go
if s.onAnalyticsSync != nil {
    s.onAnalyticsSync(payload.SessionID)
}
```

3. In handleSessionEnd, after the background ParseTranscript + token update, call:
```go
if s.onAnalyticsSync != nil {
    s.onAnalyticsSync(payload.SessionID)
}
```

4. In main.go, when creating the server, pass the analytics sync callback:
```go
// Pass to NewServer as the onAnalyticsSync parameter:
func(sessionID string) {
    syncAnalyticsToRegistry(tracker, registry, sessionID)
}
```

5. Also wire syncAnalyticsToRegistry for PreCompact handler (since it also reads tokens):
In handlePreCompact, after the background ParseTranscript goroutine, add the same onAnalyticsSync call.

6. Add CHANGELOG.md entry for v4.1.0 (or next version). Read existing CHANGELOG.md first to follow format. Add under a new version heading:

```markdown
## [4.1.0] - 2026-04-XX

### Added
- SessionEnd hook handler for instant session cleanup
- PostToolUse hook handler with throttled JSONL reads (10s)
- PreCompact/PostCompact hook handlers for compaction tracking
- StopFailure hook handler with "error" icon display
- SubagentStart hook handler for immediate subagent presence
- PostToolUseFailure hook handler for error counting
- CwdChanged hook handler for project context updates
- Binary auto-exit with configurable grace period (shutdownGracePeriod, default 30s)
- settings.local.json auto-patch for 13 HTTP hooks (start.sh)
- settings.local.json cleanup on plugin removal (stop.sh)
- "error" activity icon for API error display

### Changed
- Hook system expanded from 5 to 15 events
- Shutdown sequence: ClearActivity -> Close IPC -> Exit (clean Discord status)
- Wildcard (*) matchers for all tool-related hooks

### Removed
- JSONL fallback polling/watching (~250 lines) — replaced by hook-triggered reads
- JSONL background watcher goroutine
- StatusLineData, SessionData, JSONLMessage types from main.go
```
  </action>
  <verify>
    <automated>cd C:\Users\ktown\Projects\cc-discord-presence && go build ./... && go test -v -race ./...</automated>
  </verify>
  <acceptance_criteria>
    - server/server.go contains `onAnalyticsSync func(sessionID string)` field
    - server/server.go handlePostToolUse calls onAnalyticsSync
    - server/server.go handleSessionEnd calls onAnalyticsSync
    - main.go passes syncAnalyticsToRegistry wrapper as onAnalyticsSync
    - CHANGELOG.md contains "SessionEnd hook handler"
    - CHANGELOG.md contains "JSONL fallback polling"
    - go build ./... exits 0
    - go test -race ./... exits 0
  </acceptance_criteria>
  <done>Analytics sync wired between server hooks and registry, CHANGELOG updated, full build and tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 6 integration</name>
  <action>Human verifies the complete hook system overhaul works end-to-end by running build, tests, and manual curl checks against the daemon.</action>
  <what-built>Complete Phase 6 hook system overhaul: 8 new hook handlers, JSONL removal, auto-exit, settings.local.json deployment, analytics sync bridge</what-built>
  <how-to-verify>
    1. Build: `go build -o dsrcode .` — should compile cleanly
    2. Run tests: `go test -v -race ./...` — all should pass
    3. Start daemon: `./dsrcode` — should start without JSONL watcher messages
    4. Check hook handlers: `curl -X POST http://127.0.0.1:19460/hooks/session-end -d '{"session_id":"test","cwd":"/tmp"}'` — should return 200
    5. Check stop-failure: `curl -X POST http://127.0.0.1:19460/hooks/stop-failure -d '{"session_id":"test","error":"rate_limit"}'` — should return 200
    6. Verify start.sh patches: `bash -n scripts/start.sh` — no syntax errors
    7. Verify stop.sh cleanup: `bash -n scripts/stop.sh` — no syntax errors
    8. Check CHANGELOG.md has v4.1.0 entry
  </how-to-verify>
  <verify>
    <automated>cd C:\Users\ktown\Projects\cc-discord-presence && go build ./... && go test -race ./...</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0
    - go test -race ./... exits 0
  </acceptance_criteria>
  <done>Human approves Phase 6 integration</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analytics sync bridge | Callback from server hooks to main.go analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | R (Repudiation) | Hook event flow | mitigate | All hook handlers log via slog.Debug/Info with session ID and event type for audit trail |
| T-06-14 | D (DoS) | Analytics sync frequency | accept | syncAnalyticsToRegistry is called after throttled reads (10s min) — bounded by PostToolUse throttle |
</threat_model>

<verification>
- `go build ./...` compiles
- `go test -v -race ./...` passes
- curl to new endpoints returns 200
- No JSONL watcher messages in daemon log
</verification>

<success_criteria>
- Full integration verified: hooks -> analytics -> registry -> Discord presence
- CHANGELOG documents all Phase 6 changes
- Human verification of end-to-end functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-hook-system-overhaul-sessionend-posttooluse-precompact-hooks/06-05-SUMMARY.md`
</output>
