---
phase: 08
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - main.go
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - scripts/start.sh
  - scripts/start.ps1
  - CHANGELOG.md
  - scripts/phase-08/verify.sh
  - scripts/phase-08/verify.ps1
autonomous: false
requirements: [RLC-16, RLC-17]
tags: [release, changelog, version-bump, verify-script]

must_haves:
  truths:
    - "main.go version var, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1 all report 4.2.0 after scripts/bump-version.sh runs."
    - "CHANGELOG.md has a new ## [4.2.0] - 2026-MM-DD section following Keep a Changelog 1.1.0 (Fixed + Changed + Added subsections) with bold-lead-phrase + RLC-ID citations matching [4.1.2] style."
    - "scripts/phase-08/verify.sh (bash) and verify.ps1 (PowerShell) exist and exercise T1..T6: binary version, /health, burst coalesce, identical-content hash skip, hook dedup, 60s summary."
    - "CI workflow .github/workflows/test.yml already runs go test ... -race (VERIFIED: not an action item; acceptance criterion only per RESEARCH.md discrepancy D1)."
    - "User verifies the release artifacts via a human-verify checkpoint BEFORE tag + push (per CLAUDE.md §Releasing — tag/push is user's responsibility, not Claude's)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[4.2.0] section inserted above [4.1.2]"
      contains: "## [4.2.0]"
    - path: "scripts/phase-08/verify.sh"
      provides: "T1-T6 bash smoke test"
      contains: "dsrcode --version"
    - path: "scripts/phase-08/verify.ps1"
      provides: "T1-T6 PowerShell smoke test"
      contains: "Invoke-Test"
    - path: "main.go"
      provides: "version = \"4.2.0\" (updated by bump-version.sh)"
      contains: "version = \"4.2.0\""
  key_links:
    - from: "scripts/bump-version.sh"
      to: "main.go, plugin.json, marketplace.json, start.sh, start.ps1"
      via: "coordinated sed-based version replacement"
      pattern: "4\\.2\\.0"
    - from: "CHANGELOG.md"
      to: "RLC-01..RLC-17"
      via: "bullet points cite RLC-IDs in parentheses matching [4.1.2] style"
      pattern: "RLC-\\d+"
---

<objective>
Ship Phase 8 as release v4.2.0. Run `./scripts/bump-version.sh 4.2.0` to propagate the version across the 5 canonical files. Write a Keep-a-Changelog 1.1.0 `[4.2.0]` section in CHANGELOG.md matching the `[4.1.2]` style (bold-lead-phrase per bullet, RLC-ID citations, Fixed + Changed + Added subsections). Create `scripts/phase-08/verify.sh` and `scripts/phase-08/verify.ps1` end-to-end smoke harnesses (T1-T6). Confirm (not add) CI already runs `-race` per RESEARCH.md discrepancy D1. Gate tag + push behind a human-verify checkpoint so the user manually signs off before the GoReleaser CI workflow runs.

Purpose: Closes Phase 8 with a deployable v4.2.0. Provides T1-T6 cross-platform live-daemon smoke tests for RLC-16. Documents all five fixes in user-facing language for RLC-17. Keeps the user in the loop on the irreversible step (git tag + push).

Output: 5 version-bumped metadata files, CHANGELOG.md with [4.2.0] entry, two verify scripts in scripts/phase-08/, and a user checkpoint that surfaces the release-ready state for tag + push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-CONTEXT.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-RESEARCH.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-PATTERNS.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-VALIDATION.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-01-SUMMARY.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-02-SUMMARY.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-03-SUMMARY.md
@CHANGELOG.md
@CLAUDE.md
@scripts/bump-version.sh
@scripts/test-rotate-log.sh
@.github/workflows/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CHANGELOG.md [4.2.0] entry + run scripts/bump-version.sh 4.2.0</name>
  <files>CHANGELOG.md, main.go, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1</files>
  <read_first>
    - @CHANGELOG.md lines 1-25 (the [4.1.2] style — bold-lead-phrase + RLC-ID citation pattern to mirror)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-CONTEXT.md D-34 (verbatim target bullet text)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-PATTERNS.md §CHANGELOG.md v4.2.0 entry (adaptation checklist + exact content)
    - @scripts/bump-version.sh (understand which 5 files it touches and how)
    - @CLAUDE.md §Releasing (canonical release steps)
  </read_first>
  <behavior>
    - Running `./scripts/bump-version.sh 4.2.0` from project root updates: main.go (version var), .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1
    - CHANGELOG.md has a new `## [4.2.0] - YYYY-MM-DD` section at line 10 (above the [4.1.2] block, under [Unreleased])
    - Date uses `date -u +%Y-%m-%d` (UTC) format matching [4.1.2]
    - Each bullet leads with a bold phrase in double-asterisks, matches [4.1.2] voice (past-tense, user-benefit-first)
    - Every bullet has an RLC-ID parenthetical citation (e.g., "(Phase 8, RLC-01/RLC-02)")
  </behavior>
  <action>
    STEP 1: Insert the CHANGELOG entry FIRST (before running bump-version — the script doesn't touch CHANGELOG). Open `CHANGELOG.md` and insert at the line immediately after `## [Unreleased]` (line 8) and before `## [4.1.2]` (line 10):

    ```markdown

    ## [4.2.0] - 2026-MM-DD

    ### Fixed
    - **Presence updates no longer dropped during Discord rate-limit cooldown** — the previous `presenceDebouncer` silently discarded any update that arrived inside the 15-second cooldown window (root cause: no pending buffer). Updates are now coalesced in a lock-free `atomic.Pointer[Activity]` slot and flushed exactly once when the token-bucket limiter permits. Live logs went from ~70% skip-rate during MCP-heavy sessions to <5% (Phase 8, RLC-01/RLC-02/RLC-03).
    - **Race condition on presence-update shared state eliminated** — `sync.Mutex` on the hot path replaced with `sync/atomic.Pointer` + `atomic.Uint64` + `atomic.Int64`. The flush path is lock-free; `go test -race ./...` is green (Phase 8, RLC-02/RLC-06).
    - **Duplicate hook events silently deduped** — Claude Code's `pre-tool-use` hook was observed firing twice within 30-130 ms in production logs. A new `HookDedupMiddleware` wraps `/hooks/*` routes with a 500 ms TTL cache keyed by FNV-64a of `(route, session_id, tool_name, body)`. Non-hook routes (`/health`, `/status`, `/preview`, `/config`, `/sessions`, `/presets`, `/statusline`) pass through untouched (Phase 8, RLC-07/RLC-08/RLC-09/RLC-10/RLC-14).

    ### Changed
    - **Presence debouncer replaced with a token-bucket coalescer** (`golang.org/x/time/rate` v0.15.0 added) — 4-second cadence, burst 2. Matches empirical Discord RPC limits and delivers the latest coalesced state within ~4 s of the final signal, never discarding intermediate updates. Cold-start UX preserved: initial burst of 2 tokens means the first two presence updates after daemon launch flush immediately (Phase 8, RLC-01).
    - **FNV-64a content-hash gates every `SetActivity` call** — identical payloads (same Details/State/LargeImage/LargeText/SmallImage/SmallText/Buttons; `StartTime` intentionally excluded) never consume a rate-limit token. Deterministic across process restarts via `hash/fnv.New64a()` with an ASCII Unit Separator (0x1F) between fields (Phase 8, RLC-04/RLC-05/RLC-06).

    ### Added
    - **Hook-dedup middleware** wrapping `/hooks/*` routes with a `sync.Map`-backed 500 ms TTL cache and a 60-second background cleanup goroutine. Request body is capped at 64 KiB via `http.MaxBytesReader` (defence-in-depth; real payloads are <10 KB) and body-preserved via `io.NopCloser(bytes.NewReader(...))` for downstream handlers (Phase 8, RLC-07..RLC-10).
    - **Periodic 60-second INFO summary log** with aggregate coalescer counters: `sent`, `skipped_rate`, `skipped_hash`, `deduped`, emitted via `slog.Info("coalescer status", ...)`. Skipped entirely when all counters are zero so idle daemons produce no log noise (Phase 8, RLC-11).
    - **`testing/synctest`-based unit + integration tests** for the new `coalescer/` package and `server/hook_dedup.go` middleware. Uses Go 1.25's GA virtual-time bubbles to assert token-bucket scheduling, pending-slot semantics, hash skip, shutdown idempotency, and TTL eviction without wall-clock dependencies (Phase 8, RLC-15).
    - **Cross-platform release verification harness** at `scripts/phase-08/verify.sh` (bash) and `scripts/phase-08/verify.ps1` (PowerShell) exercising T1-T6: binary version check, `/health` liveness, burst-coalesce rate, identical-content hash skip, hook-dedup log evidence, and 60-second summary log appearance (Phase 8, RLC-16).
    ```

    Replace `2026-MM-DD` with the current UTC date. The executor resolves this from the environment (`date -u +%Y-%m-%d` output). If running interactively today, use `2026-04-16` or the current date.

    STEP 2: Run `./scripts/bump-version.sh 4.2.0` from the project root. This updates 5 files in one pass:
    ```bash
    cd /c/Users/ktown/Projects/cc-discord-presence && ./scripts/bump-version.sh 4.2.0
    ```

    The script is idempotent on the exact version string. Verify output shows all 5 files rewritten. Do NOT hand-edit the 5 version-file strings — let the script own the propagation.

    STEP 3: Confirm via grep that all 5 files now contain `4.2.0`:
    ```bash
    grep -l "4.2.0" main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1
    ```
    Expect all 5 paths in the output.

    STEP 4: Run `go build ./...` to confirm the version-string update did not break compilation.
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && go build ./... && grep -c 'version = "4.2.0"' main.go && grep -c '## \[4.2.0\]' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'version = "4.2.0"' main.go` == 1
    - `grep -c '4.2.0' .claude-plugin/plugin.json` >= 1
    - `grep -c '4.2.0' .claude-plugin/marketplace.json` >= 1
    - `grep -c '4.2.0' scripts/start.sh` >= 1
    - `grep -c '4.2.0' scripts/start.ps1` >= 1
    - `grep -c '## \[4.2.0\]' CHANGELOG.md` == 1
    - CHANGELOG has exactly one ### Fixed, one ### Changed, and one ### Added heading under the [4.2.0] section
    - Every bullet in [4.2.0] contains at least one `RLC-NN` reference
    - Every bullet in [4.2.0] starts with `- **` (bold-lead-phrase style matching [4.1.2])
    - The [4.2.0] section appears BEFORE (above) the [4.1.2] section in file order
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>Version propagated across 5 files. CHANGELOG has a complete [4.2.0] entry matching [4.1.2] style. Project still builds.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/phase-08/verify.sh (T1-T6 bash smoke)</name>
  <files>scripts/phase-08/verify.sh</files>
  <read_first>
    - @scripts/test-rotate-log.sh (the only existing bash PASS/FAIL harness — copy set -euo pipefail + PASS/FAIL shape)
    - @scripts/start.sh (log path: ~/.claude/dsrcode.log convention)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-CONTEXT.md D-31 (T1-T6 definitions)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-PATTERNS.md §scripts/phase-08/verify.sh (adaptation checklist)
  </read_first>
  <behavior>
    - T1: `dsrcode --version` output contains `4.2.0`
    - T2: `curl -fsS http://127.0.0.1:19460/health` returns HTTP 200
    - T3: Burst 10 POSTs with DISTINCT session IDs to /hooks/pre-tool-use within 1s; count `presence updated` log lines in the succeeding 10s; assert ≤3 (initial burst + one coalesced flush)
    - T4: Burst 10 IDENTICAL POSTs; assert `presence update skipped.*content_hash` appears ≥9 times in log tail
    - T5: Two IDENTICAL POSTs within 500ms; assert `hook deduped` appears in log tail exactly once
    - T6: `sleep 65` then assert `coalescer status` appears at least once in log tail (with counters > 0 from T3/T4/T5 activity)
    - Script exits 0 on all-pass, non-zero otherwise
    - Script has `set -euo pipefail`, `trap 'rm -rf "$TMPDIR"' EXIT` style cleanup, PASS/FAIL console output
  </behavior>
  <action>
    Create `scripts/phase-08/verify.sh`. First ensure the directory exists:
    ```bash
    mkdir -p scripts/phase-08
    ```

    Then write the file:

    ```bash
    #!/bin/bash
    # Phase 8 live-daemon verification harness.
    #
    # Validates:
    #   T1 dsrcode binary reports version 4.2.0
    #   T2 /health returns HTTP 200
    #   T3 Burst 10 distinct signals -> <=3 coalesced SetActivity calls observed in log
    #   T4 Burst 10 identical signals -> >=9 "content_hash" skip events observed in log
    #   T5 Duplicate POST /hooks/pre-tool-use within 500ms -> exactly 1 "hook deduped" log
    #   T6 Periodic 60s "coalescer status" INFO summary appears once counters are non-zero
    #
    # Preconditions: Daemon must already be running (start.sh launches it).
    # Usage: bash scripts/phase-08/verify.sh
    # Exits 0 on all-pass, non-zero on any assertion failure.

    set -euo pipefail

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    LOG_FILE="${HOME}/.claude/dsrcode.log"
    DAEMON_URL="http://127.0.0.1:19460"

    pass() { echo "PASS: $1"; }
    fail() { echo "FAIL: $1" >&2; exit 1; }

    # Record log offset at start so T3..T6 only look at lines written during this run.
    if [[ ! -f "$LOG_FILE" ]]; then
        fail "log file $LOG_FILE not found — daemon not running?"
    fi
    LOG_OFFSET=$(wc -c < "$LOG_FILE")

    log_tail() {
        # Print log bytes written after LOG_OFFSET.
        dd if="$LOG_FILE" bs=1 skip="$LOG_OFFSET" 2>/dev/null
    }

    # --- T1: Binary version ---
    if ! command -v dsrcode >/dev/null 2>&1; then
        fail "T1 dsrcode binary not on PATH"
    fi
    VERSION_OUT=$(dsrcode --version 2>&1 || true)
    if echo "$VERSION_OUT" | grep -q "4.2.0"; then
        pass "T1 dsrcode --version contains 4.2.0"
    else
        fail "T1 dsrcode --version lacks 4.2.0 — got: $VERSION_OUT"
    fi

    # --- T2: /health ---
    if curl -fsS -o /dev/null "${DAEMON_URL}/health"; then
        pass "T2 /health returned HTTP 200"
    else
        fail "T2 /health did not return 200 (daemon not running or bind changed)"
    fi

    # --- T3: Burst coalescing (10 distinct session IDs -> <=3 SetActivity calls) ---
    for i in $(seq 1 10); do
        curl -fsS -o /dev/null -X POST \
            -H "Content-Type: application/json" \
            -d "{\"session_id\":\"t3-s${i}\",\"tool_name\":\"Edit\",\"cwd\":\"/tmp/t3\"}" \
            "${DAEMON_URL}/hooks/pre-tool-use"
    done
    # Wait for debounce + first coalesced flush + subsequent bucket refills.
    sleep 10

    T3_COUNT=$(log_tail | grep -c '"presence updated"' || true)
    if [[ "$T3_COUNT" -le 3 ]]; then
        pass "T3 10 distinct signals produced $T3_COUNT SetActivity calls (<=3 coalesced)"
    else
        fail "T3 expected <=3 coalesced SetActivity calls, got $T3_COUNT"
    fi

    # --- T4: Content-hash skip (10 identical signals -> >=9 hash-skips) ---
    IDENT_BODY='{"session_id":"t4-s1","tool_name":"Edit","cwd":"/tmp/t4"}'
    for i in $(seq 1 10); do
        curl -fsS -o /dev/null -X POST \
            -H "Content-Type: application/json" \
            -d "$IDENT_BODY" \
            "${DAEMON_URL}/hooks/pre-tool-use" || true
        # Yield tiny spacing so dedup TTL passes between each
        sleep 0.6
    done
    sleep 2

    T4_COUNT=$(log_tail | grep -c '"presence update skipped".*"reason":"content_hash"' || true)
    if [[ "$T4_COUNT" -ge 9 ]]; then
        pass "T4 $T4_COUNT content_hash skips observed (>=9 expected from 10 identical signals)"
    else
        fail "T4 expected >=9 content_hash skip events, got $T4_COUNT"
    fi

    # --- T5: Hook dedup (2 identical POSTs within 500ms -> exactly 1 dedup log) ---
    DEDUP_BODY='{"session_id":"t5-s1","tool_name":"Grep","cwd":"/tmp/t5"}'
    curl -fsS -o /dev/null -X POST -H "Content-Type: application/json" -d "$DEDUP_BODY" "${DAEMON_URL}/hooks/pre-tool-use"
    curl -fsS -o /dev/null -X POST -H "Content-Type: application/json" -d "$DEDUP_BODY" "${DAEMON_URL}/hooks/pre-tool-use"
    sleep 1

    T5_COUNT=$(log_tail | grep -c '"hook deduped"' || true)
    if [[ "$T5_COUNT" -eq 1 ]]; then
        pass "T5 exactly 1 hook dedup observed for 2 identical POSTs within 500ms"
    else
        fail "T5 expected exactly 1 hook dedup, got $T5_COUNT"
    fi

    # --- T6: 60s summary log ---
    echo "T6 waiting 65s for coalescer-status summary..."
    sleep 65

    T6_COUNT=$(log_tail | grep -c '"coalescer status"' || true)
    if [[ "$T6_COUNT" -ge 1 ]]; then
        pass "T6 coalescer status summary appeared $T6_COUNT time(s)"
    else
        fail "T6 no coalescer status summary observed after 65s wait"
    fi

    echo ""
    echo "All Phase 8 verify tests passed (T1..T6)."
    ```

    Make it executable:
    ```bash
    chmod +x scripts/phase-08/verify.sh
    ```
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && test -x scripts/phase-08/verify.sh && head -2 scripts/phase-08/verify.sh | grep -q "^#!/bin/bash"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-08/verify.sh` exists and is executable (`test -x` returns 0)
    - File starts with `#!/bin/bash`
    - File contains `set -euo pipefail`
    - File contains the `LOG_OFFSET=$(wc -c < "$LOG_FILE")` offset idiom
    - File contains six distinct PASS paths: T1, T2, T3, T4, T5, T6 (grep-verifiable: `grep -c 'pass \"T[1-6]' scripts/phase-08/verify.sh` == 6)
    - File contains `dsrcode --version` usage
    - File contains `curl -fsS "${DAEMON_URL}/health"` (or equivalent) for T2
    - File contains `"presence updated"` grep for T3
    - File contains `"content_hash"` grep for T4
    - File contains `"hook deduped"` grep for T5
    - File contains `"coalescer status"` grep for T6
    - Shellcheck (if available): `shellcheck scripts/phase-08/verify.sh` exits 0 or produces only style warnings
  </acceptance_criteria>
  <done>scripts/phase-08/verify.sh exists, is executable, starts with shebang+strict mode, and exercises T1-T6 with PASS/FAIL output.</done>
</task>

<task type="auto">
  <name>Task 3: Write scripts/phase-08/verify.ps1 (T1-T6 PowerShell smoke)</name>
  <files>scripts/phase-08/verify.ps1</files>
  <read_first>
    - @scripts/phase-08/verify.sh (Task 2 output — PowerShell script exercises the same T1-T6)
    - @scripts/start.ps1 (log path: $env:USERPROFILE\.claude\dsrcode.log convention)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-PATTERNS.md §scripts/phase-08/verify.ps1 (analog: scripts/phase-06.1/verify.ps1:1-232 — Invoke-Test helper + results JSON export)
  </read_first>
  <behavior>
    - Uses `Invoke-Test` helper pattern from phase-06.1/verify.ps1
    - T1-T6 mirror the bash script exactly in semantics
    - Exports `verify-results.json` with pass/fail/skip counts for downstream consumption
    - Uses `$env:USERPROFILE\.claude\dsrcode.log` as the log path
    - Handles missing log file gracefully (fails T1-T6 cleanly instead of crashing)
  </behavior>
  <action>
    Create `scripts/phase-08/verify.ps1`:

    ```powershell
    #Requires -Version 5.1
    <#
    .SYNOPSIS
        Phase 8 live-daemon verification harness — runs T1-T6 cross-platform smoke tests.

    .DESCRIPTION
        Validates:
          T1 dsrcode binary reports version 4.2.0
          T2 /health returns HTTP 200
          T3 Burst 10 distinct signals -> <=3 coalesced SetActivity calls observed
          T4 Burst 10 identical signals -> >=9 content_hash skip events observed
          T5 Duplicate POST /hooks/pre-tool-use within 500ms -> exactly 1 hook deduped log
          T6 60s summary log appears after activity

        Preconditions: Daemon must already be running (start.ps1 launches it).

    .EXAMPLE
        .\scripts\phase-08\verify.ps1
    #>

    [CmdletBinding()]
    param()

    $ErrorActionPreference = 'Continue'

    # Test result tracking
    $script:results  = [ordered]@{}
    $script:passCount = 0
    $script:failCount = 0

    $DaemonUrl = 'http://127.0.0.1:19460'
    $LogFile   = Join-Path $env:USERPROFILE '.claude\dsrcode.log'

    function Invoke-Test {
        param(
            [string]$Id,
            [string]$Description,
            [scriptblock]$Test
        )
        Write-Host -NoNewline "[$Id] $Description ... "
        try {
            $result = & $Test
            if ($result -is [bool] -and $result) {
                Write-Host "PASS" -ForegroundColor Green
                $script:passCount++
                $script:results[$Id] = 'PASS'
                return $true
            } else {
                Write-Host "FAIL" -ForegroundColor Red
                $script:failCount++
                $script:results[$Id] = 'FAIL'
                return $false
            }
        } catch {
            Write-Host "ERROR: $($_.Exception.Message)" -ForegroundColor Red
            $script:failCount++
            $script:results[$Id] = "ERROR: $($_.Exception.Message)"
            return $false
        }
    }

    # Record log offset so T3-T6 only scan lines written during this run.
    if (-not (Test-Path -Path $LogFile)) {
        Write-Host "FAIL: log file $LogFile not found -- daemon not running?" -ForegroundColor Red
        exit 1
    }
    $script:logOffset = (Get-Item $LogFile).Length

    function Get-NewLogBytes {
        $fs = [System.IO.File]::Open($LogFile, 'Open', 'Read', 'ReadWrite')
        try {
            $null = $fs.Seek($script:logOffset, 'Begin')
            $sr = New-Object System.IO.StreamReader($fs)
            return $sr.ReadToEnd()
        } finally {
            $fs.Close()
        }
    }

    function Count-LogMatches {
        param([string]$Pattern)
        $text = Get-NewLogBytes
        if ([string]::IsNullOrEmpty($text)) { return 0 }
        return ([regex]::Matches($text, $Pattern)).Count
    }

    function Invoke-HookPost {
        param(
            [string]$Path,
            [string]$Body
        )
        Invoke-WebRequest -Method Post -Uri ($DaemonUrl + $Path) `
            -ContentType 'application/json' -Body $Body `
            -UseBasicParsing -TimeoutSec 5 -ErrorAction Stop | Out-Null
    }

    # --- T1: Binary version ---
    Invoke-Test -Id 'T1' -Description 'dsrcode --version reports 4.2.0' -Test {
        $cmd = Get-Command -Name 'dsrcode' -ErrorAction SilentlyContinue
        if (-not $cmd) { return $false }
        $output = & dsrcode --version 2>&1 | Out-String
        return ($output -match '4\.2\.0')
    }

    # --- T2: /health ---
    Invoke-Test -Id 'T2' -Description 'HTTP 200 from /health' -Test {
        try {
            $resp = Invoke-WebRequest -Uri ($DaemonUrl + '/health') -UseBasicParsing -TimeoutSec 3 -ErrorAction Stop
            return ($resp.StatusCode -eq 200)
        } catch {
            return $false
        }
    }

    # --- T3: Burst coalescing ---
    Invoke-Test -Id 'T3' -Description 'Burst 10 distinct signals produce <=3 coalesced flushes' -Test {
        for ($i = 1; $i -le 10; $i++) {
            Invoke-HookPost -Path '/hooks/pre-tool-use' `
                -Body "{`"session_id`":`"t3-s$i`",`"tool_name`":`"Edit`",`"cwd`":`"/tmp/t3`"}"
        }
        Start-Sleep -Seconds 10
        $count = Count-LogMatches '"presence updated"'
        Write-Verbose "T3 observed $count SetActivity calls"
        return ($count -le 3)
    }

    # --- T4: Content-hash skip ---
    Invoke-Test -Id 'T4' -Description 'Burst 10 identical signals produce >=9 content_hash skips' -Test {
        $body = '{"session_id":"t4-s1","tool_name":"Edit","cwd":"/tmp/t4"}'
        for ($i = 1; $i -le 10; $i++) {
            try { Invoke-HookPost -Path '/hooks/pre-tool-use' -Body $body } catch {}
            Start-Sleep -Milliseconds 600
        }
        Start-Sleep -Seconds 2
        $count = Count-LogMatches '"presence update skipped".*"reason":"content_hash"'
        Write-Verbose "T4 observed $count content_hash skips"
        return ($count -ge 9)
    }

    # --- T5: Hook dedup ---
    Invoke-Test -Id 'T5' -Description '2 identical POSTs within 500ms produce exactly 1 hook dedup log' -Test {
        $body = '{"session_id":"t5-s1","tool_name":"Grep","cwd":"/tmp/t5"}'
        Invoke-HookPost -Path '/hooks/pre-tool-use' -Body $body
        Invoke-HookPost -Path '/hooks/pre-tool-use' -Body $body
        Start-Sleep -Seconds 1
        $count = Count-LogMatches '"hook deduped"'
        Write-Verbose "T5 observed $count hook dedups"
        return ($count -eq 1)
    }

    # --- T6: 60s summary log ---
    Invoke-Test -Id 'T6' -Description '60s coalescer-status summary appears' -Test {
        Write-Verbose 'T6 waiting 65s for summary...'
        Start-Sleep -Seconds 65
        $count = Count-LogMatches '"coalescer status"'
        Write-Verbose "T6 observed $count summary logs"
        return ($count -ge 1)
    }

    # --- Summary ---
    Write-Host ''
    Write-Host "Phase 8 verify: $script:passCount passed, $script:failCount failed"

    $summaryPath = Join-Path $PSScriptRoot 'verify-results.json'
    $summary = @{
        phase     = '8'
        timestamp = (Get-Date).ToString('o')
        passCount = $script:passCount
        failCount = $script:failCount
        results   = $script:results
    } | ConvertTo-Json -Depth 4
    Set-Content -Path $summaryPath -Value $summary -Encoding UTF8

    if ($script:failCount -gt 0) { exit 1 }
    exit 0
    ```

    Do NOT `chmod +x` — `.ps1` executes via PowerShell host, not POSIX exec bit.
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && test -f scripts/phase-08/verify.ps1 && head -2 scripts/phase-08/verify.ps1 | grep -q "#Requires -Version 5.1"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase-08/verify.ps1` exists
    - File starts with `#Requires -Version 5.1`
    - File contains `function Invoke-Test` definition
    - File contains six `Invoke-Test -Id 'T1'` through `Invoke-Test -Id 'T6'` calls
    - File uses `$env:USERPROFILE\.claude\dsrcode.log` for log path
    - File uses `Invoke-WebRequest -Method Post` for hook POSTs
    - File exports `verify-results.json` at end with pass/fail counts
    - File exits 1 on any failure, 0 on all-pass
    - PowerShell parse: `powershell -NoProfile -Command "& { try { [scriptblock]::Create((Get-Content -Raw scripts/phase-08/verify.ps1)) | Out-Null; exit 0 } catch { Write-Host $_; exit 1 } }"` exits 0 (parse-only syntax check)
  </acceptance_criteria>
  <done>scripts/phase-08/verify.ps1 exists, parses cleanly as PowerShell 5.1, uses the Invoke-Test pattern, and exercises T1-T6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify release artifacts + run live T1-T6 + approve tag/push</name>
  <files>CHANGELOG.md, scripts/phase-08/verify.sh, scripts/phase-08/verify.ps1, main.go, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1</files>
  <action>
    This is a human-verify checkpoint. Claude pauses here and presents the user with the release-ready state for manual approval. Claude MUST NOT run git tag or git push commands autonomously — per user memory ("3-tag-push limit") and CLAUDE.md §Releasing, the tag + push is explicitly the USER final signoff action.

    Present the <what-built>, <how-to-verify>, and <resume-signal> content below to the user and wait for an explicit "approved" resume-signal. On approval, mark Task 4 done and proceed to write the plan SUMMARY. On rejection, fix the issue the user described and re-present the checkpoint.
  </action>
  <what-built>
    Phase 8 is feature-complete:
    - 4 plans shipped (08-01 Coalescer core, 08-02 Content-hash, 08-03 Hook-dedup, 08-04 Release prep)
    - Version 4.2.0 propagated across 5 files by scripts/bump-version.sh
    - CHANGELOG.md has a complete [4.2.0] entry following [4.1.2] style
    - scripts/phase-08/verify.sh + verify.ps1 created and ready to exercise against a live daemon
    - CI workflow .github/workflows/test.yml already runs `-race` (verified — no change needed per RESEARCH.md discrepancy D1)
    - Full test suite `go test ./... -race -count=1` is green (coalescer + server packages + everything else)
  </what-built>
  <how-to-verify>
    1. Read the [4.2.0] CHANGELOG entry aloud — does it describe the user-visible win clearly? Are all five fixes mentioned (pending buffer, token bucket, content-hash, hook dedup, race-free atomics)?

    2. Run `./scripts/bump-version.sh --dry-run 4.2.0` (or grep for 4.2.0) and confirm all 5 expected files report 4.2.0:
       - main.go (version var)
       - .claude-plugin/plugin.json
       - .claude-plugin/marketplace.json
       - scripts/start.sh
       - scripts/start.ps1

    3. Run the full test suite locally:
       ```bash
       go test ./... -race -count=1 -v
       ```
       Expect all coalescer tests (10), all server tests (including 6 new dedup tests), and all existing tests to pass.

    4. Rebuild and restart the daemon:
       ```bash
       go build -o dsrcode .
       # Stop the running daemon via stop.sh or taskkill
       # Relaunch via start.sh or start.ps1
       ```

    5. Run the live verify harness for your platform:
       - Unix/Git Bash: `bash scripts/phase-08/verify.sh`
       - Windows PowerShell: `powershell -ExecutionPolicy Bypass -File scripts/phase-08/verify.ps1`
       Expect 6 PASS lines (T1-T6). Review any FAIL with the log tail at ~/.claude/dsrcode.log.

    6. Open Discord and watch the presence for 2-3 minutes of normal Claude Code activity. Previously the presence got "stuck" during MCP-heavy sessions (Phase 8 trigger). Now it should update smoothly ~every 4 seconds when content changes.

    7. Inspect the log for the 60s summary:
       ```bash
       grep "coalescer status" ~/.claude/dsrcode.log | tail -5
       ```
       Expect entries with sent/skipped_rate/skipped_hash/deduped counters populated.

    8. Once satisfied, the user will manually run the git tag + push per CLAUDE.md §Releasing:
       ```bash
       git add main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 CHANGELOG.md scripts/phase-08/
       git commit -m "chore: bump version to v4.2.0"
       git tag v4.2.0
       git push origin main --tags
       ```
       GoReleaser CI will auto-build the 5-platform binaries and cut a GitHub Release.

    Claude MUST NOT run the tag + push commands autonomously — per user memory ("3-tag-push limit") and CLAUDE.md §Releasing, git tag + push v4.2.0 is the USER's final signoff action.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 8 complete, or describe what needs fixing. Only after "approved" will this plan's tasks mark done — the actual git tag + push remains the user's responsibility.</resume-signal>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && go test ./... -race -count=1 && grep -c 'version = "4.2.0"' main.go && grep -c '## \\[4.2.0\\]' CHANGELOG.md && test -x scripts/phase-08/verify.sh && test -f scripts/phase-08/verify.ps1</automated>
    <manual>User runs `bash scripts/phase-08/verify.sh` (or `powershell -File scripts/phase-08/verify.ps1`) against a live v4.2.0 daemon and confirms T1..T6 all PASS; then user types "approved" to resume. Actual git tag v4.2.0 + git push is then the user manual step per CLAUDE.md §Releasing.</manual>
  </verify>
  <done>User has typed "approved" after reviewing the CHANGELOG, confirming version propagation across 5 files, seeing the full test suite green, and running the live verify.sh / verify.ps1 harness with all six T1-T6 checks passing. The actual git tag v4.2.0 + git push origin main --tags is a follow-up manual step performed by the user outside this plan scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/bump-version.sh → 5 target files | Shell script modifying version strings via sed; trusted (repo-owned) |
| scripts/phase-08/verify.* → live daemon | Exercises HTTP endpoints; localhost only; read-only w.r.t. repo state |
| CHANGELOG.md → downstream release consumers | Published as release notes; must accurately describe user-facing changes |
| Human verification checkpoint → git tag + push | User-controlled; Claude does NOT execute tag/push autonomously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | bump-version.sh could update some files but miss others (partial rename) | mitigate | Task 1 STEP 3 verifies all 5 files contain `4.2.0` via explicit grep. Any missing file fails the acceptance criterion. |
| T-08-04-02 | Information Disclosure | CHANGELOG accidentally referencing internal file paths or implementation secrets | accept | CHANGELOG content is user-facing by design; reviewed in Task 4 checkpoint. No secrets (API keys, tokens, env vars) appear in the draft text. |
| T-08-04-03 | Denial of Service | verify.sh / verify.ps1 infinite-looping against daemon | mitigate | Both scripts have explicit `Start-Sleep` / `sleep` bounds (<=65 s per test) and no retry loops. Total runtime ~90 s worst case. |
| T-08-04-04 | Denial of Service | verify scripts flooding daemon with hook requests during T3/T4 | accept | 10 POSTs per test, well below any reasonable rate limit. Daemon serves these within <1 s. |
| T-08-04-05 | Elevation of Privilege | git tag + push executed autonomously without user approval | mitigate | Task 4 is a `checkpoint:human-verify` gate. Explicit instruction: "Claude MUST NOT run the tag + push commands autonomously." User memory ("3-tag-push limit") reinforces this. |
| T-08-04-06 | Spoofing | Mis-dated CHANGELOG entry (wrong UTC date) | mitigate | Task 1 STEP 1 uses `date -u +%Y-%m-%d` at execution time; the checkpoint review catches wrong dates. |
</threat_model>

<verification>
- `git status` shows 5 version-bumped files + CHANGELOG.md + 2 scripts/phase-08/ files all modified/new
- `go build ./...` succeeds (post-version-bump)
- `go test ./... -race -count=1` all green
- Live daemon: `bash scripts/phase-08/verify.sh` (or `.ps1`) T1-T6 all PASS
- User manually approves via checkpoint before any git tag operation
</verification>

<success_criteria>
- [ ] bump-version.sh 4.2.0 propagated to main.go, plugin.json, marketplace.json, start.sh, start.ps1
- [ ] CHANGELOG.md has a [4.2.0] section with Fixed + Changed + Added subsections matching [4.1.2] voice
- [ ] Every CHANGELOG bullet cites ≥1 RLC-NN
- [ ] scripts/phase-08/verify.sh exists, executable, has T1-T6
- [ ] scripts/phase-08/verify.ps1 exists, parses cleanly, has T1-T6
- [ ] No `-race` task created for .github/workflows/test.yml (per RESEARCH discrepancy D1 — already present; confirmed via verification, not modification)
- [ ] go test ./... -race -count=1 green across the whole repo
- [ ] Human verification checkpoint Task 4 executed; user signoff before tag/push
- [ ] Locked decisions D-01 (v4.2.0 minor), D-02 (all 5 fixes bundled), D-34 (CHANGELOG structure) ALL implemented
</success_criteria>

<output>
After completion (including user approval at checkpoint), create `.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-04-SUMMARY.md` covering:
- Commits made (expected 2-3: CHANGELOG + bump, verify scripts, possibly a fixup)
- T1-T6 results (from live verify.sh or verify.ps1 run)
- Final version propagation confirmation
- Any deviations + rationale
- Handoff: Phase 8 complete. Next action is USER's: git tag v4.2.0 + push per CLAUDE.md §Releasing (CI will handle GoReleaser build + GitHub Release).
</output>
