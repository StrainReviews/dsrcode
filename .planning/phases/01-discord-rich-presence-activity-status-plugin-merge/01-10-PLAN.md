---
phase: 01-discord-rich-presence-activity-status-plugin-merge
plan: 10
type: execute
wave: 5
depends_on: ["01-08"]
files_modified:
  - scripts/statusline-wrapper.sh
autonomous: false
requirements: [D-36, D-37, D-38, D-45, D-46, D-47, D-56]
user_setup:
  - service: discord-developer-portal
    why: "Discord Rich Presence requires a registered Application"
    env_vars:
      - name: DISCORD_CLIENT_ID
        source: "Discord Developer Portal -> Applications -> DSR Code -> Application ID"
    dashboard_config:
      - task: "Create Discord Application named 'DSR Code'"
        location: "https://discord.com/developers/applications -> New Application"
      - task: "Upload 10 Rich Presence assets (1024x1024 PNG)"
        location: "Discord Developer Portal -> Applications -> DSR Code -> Rich Presence -> Art Assets"

must_haves:
  truths:
    - "Statusline wrapper POSTs JSON to HTTP server and passes through to next handler"
    - "Discord Application 'DSR Code' is created with 10 icon assets uploaded"
    - "Icons are generated at 1024x1024 PNG resolution"
  artifacts:
    - path: "scripts/statusline-wrapper.sh"
      provides: "Statusline HTTP POST wrapper per D-36/D-38"
      contains: "curl"
  key_links:
    - from: "scripts/statusline-wrapper.sh"
      to: "server/server.go"
      via: "POST /statusline"
      pattern: "19460/statusline"
---

<objective>
Create the statusline wrapper script and set up Discord Application with icon assets.

Purpose: The statusline wrapper enables real-time model/token/cost data to flow from Claude Code's statusline to the Go server. The Discord Application is required for Rich Presence to work. Icons are needed for the activity display.
Output: statusline-wrapper.sh, generated icons uploaded to Discord.
</objective>

<execution_context>
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ktown/Projects/StrainReviewsScanner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-discord-rich-presence-activity-status-plugin-merge/01-CONTEXT.md

**IMPORTANT:** All code work happens in `C:\Users\ktown\Projects\cc-discord-presence`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create statusline wrapper script</name>
  <files>
    C:/Users/ktown/Projects/cc-discord-presence/scripts/statusline-wrapper.sh
  </files>
  <read_first>
    C:/Users/ktown/Projects/cc-discord-presence/scripts/start.sh
  </read_first>
  <action>
Create `scripts/statusline-wrapper.sh` per D-36/D-37/D-38:

This script replaces the file-based statusline data exchange. It:
1. Reads JSON from stdin (Claude Code statusline data)
2. POSTs it to the Go HTTP server at localhost:19460/statusline
3. Passes the original JSON through to stdout (for the next statusline handler, e.g., GSD)

```bash
#!/bin/bash
# Statusline wrapper for cc-discord-presence
# Reads statusline JSON from stdin, POSTs to HTTP server, passes through to stdout.
# Per D-36: replaces file-based intermediary (discord-presence-data.json)
# Per D-38: leitet stdin durch an naechste Statusline (GSD etc.)

read -r json

# POST to Go HTTP server (fire-and-forget, non-blocking)
# Uses curl with 1s timeout so it doesn't block the statusline chain
echo "$json" | curl -s -X POST \
  -H "Content-Type: application/json" \
  -d @- \
  --max-time 1 \
  http://127.0.0.1:19460/statusline > /dev/null 2>&1 &

# Pass through to next handler (critical: must always output regardless of curl success)
echo "$json"
```

Make the script executable: `chmod +x scripts/statusline-wrapper.sh`
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && bash -n scripts/statusline-wrapper.sh && test -x scripts/statusline-wrapper.sh && echo "Valid and executable"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/statusline-wrapper.sh exists and is executable
    - Script reads from stdin with `read -r json`
    - Script POSTs to http://127.0.0.1:19460/statusline via curl
    - Script echoes original JSON to stdout (passthrough)
    - curl runs in background (& operator) so it doesn't block
    - curl has --max-time 1 for timeout protection
    - `bash -n scripts/statusline-wrapper.sh` passes (valid syntax)
  </acceptance_criteria>
  <done>Statusline wrapper created, passes JSON to HTTP server and through to next handler.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Discord Application "DSR Code" and upload icon assets</name>
  <action>
User must manually create the Discord Application and upload icons. Claude will generate icons via fal.ai MCP and provide step-by-step instructions.

1. Generate 10 icons at 1024x1024 PNG via fal.ai MCP with prompt: "A minimal, modern icon for [description], 1024x1024, flat design, dark background with green (#22c55e) accent, suitable for Discord Rich Presence small image"
2. Icons to generate: dsr-code (main app icon), coding, terminal, searching, thinking, reading, idle, starting, multi-session, streaming
3. Save generated PNGs to a temporary directory

Then guide user through:
- Create Discord Application at https://discord.com/developers/applications named "DSR Code" per D-16
- Copy Application ID and save to ~/.claude/discord-presence-config.json as discordClientId
- Upload all 10 PNGs to Rich Presence Art Assets
- Asset keys must match code: dsr-code, coding, terminal, searching, thinking, reading, idle, starting, multi-session, streaming
  </action>
  <verify>
    <automated>echo "Manual verification required — Discord Developer Portal"</automated>
  </verify>
  <acceptance_criteria>
    - Discord Application "DSR Code" exists in Developer Portal
    - Application ID is saved in ~/.claude/discord-presence-config.json
    - 10 icon assets uploaded with correct asset key names
    - Asset keys match: dsr-code, coding, terminal, searching, thinking, reading, idle, starting, multi-session, streaming
  </acceptance_criteria>
  <done>Discord Application created, 10 icon assets uploaded, Application ID configured.</done>
  <resume-signal>Provide the Discord Application ID and confirm all 10 icons are uploaded. Type "done" with the Application ID.</resume-signal>
</task>

</tasks>

<verification>
Statusline wrapper passes shell syntax check. Discord Application exists with 10 assets.
</verification>

<success_criteria>
- statusline-wrapper.sh passes JSON through while POSTing to server
- Discord Application "DSR Code" created
- 10 icon assets uploaded at 1024x1024
- Application ID saved to config
</success_criteria>

<output>
After completion, create `.planning/phases/01-discord-rich-presence-activity-status-plugin-merge/01-10-SUMMARY.md`
</output>
