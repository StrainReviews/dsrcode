---
phase: 9
plan: 2
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - CHANGELOG.md
  - main.go
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - scripts/start.sh
  - scripts/start.ps1
  - scripts/phase-09/verify.sh
  - scripts/phase-09/verify.ps1
autonomous: false
requirements: [D-06, D-09]
tags: [release, changelog, version-bump, verify-script, v4.2.1, hotfix, human-verify]

must_haves:
  truths:
    - "main.go version var, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1 all report 4.2.1 after scripts/bump-version.sh runs."
    - "CHANGELOG.md has a new ## [4.2.1] - 2026-04-18 section inserted BEFORE [4.2.0] and AFTER [Unreleased], with Fixed / Changed / Added subsections matching [4.1.2] voice (bold-lead-phrase + en-dash + Phase-ID citations + inline code symbols)."
    - "CHANGELOG [4.2.1] Fixed bullet cites the live-incident forensic anchor (session 2fe1b32a-ea1d-464f-8cac-375a4fe709c9, PID 5692, 2m25s elapsed, 8 skipping-removal debug lines)."
    - "scripts/phase-09/verify.sh (bash) + verify.ps1 (PowerShell) exist mirroring Phase-8 structure with Phase-9-specific T-assertions: T1 version=4.2.1, T2 /health, T3 SourceClaude UUID session survives >=150s stale-check sweep (0 removal log lines)."
    - "User runs the live verify harness against a freshly built v4.2.1 daemon AND types 'approved' at the human-verify checkpoint BEFORE any git tag + push."
    - "Claude does NOT execute git tag v4.2.1 or git push origin main --tags — per CLAUDE.md Releasing and the 3-tag-push-limit memory, tagging + pushing is the USER's exclusive follow-up action."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[4.2.1] section inserted above [4.2.0]"
      contains: "## [4.2.1]"
    - path: "scripts/phase-09/verify.sh"
      provides: "T1-T3+ bash smoke harness (version, /health, SourceClaude UUID stale-check absence)"
      contains: "verify09-"
    - path: "scripts/phase-09/verify.ps1"
      provides: "T1-T3+ PowerShell parity"
      contains: "Invoke-Test"
    - path: "main.go"
      provides: "version = 4.2.1 (updated by bump-version.sh)"
      contains: "version = \"4.2.1\""
  key_links:
    - from: "scripts/bump-version.sh"
      to: "main.go, plugin.json, marketplace.json, start.sh, start.ps1"
      via: "coordinated sed-based version replacement"
      pattern: "4\\.2\\.1"
    - from: "CHANGELOG.md [4.2.1]"
      to: "dsrcode.log 2026-04-18 11:37-11:44 live incident"
      via: "forensic anchor in Fixed bullet (session 2fe1b32a + PID 5692 + 2m25s)"
      pattern: "2fe1b32a-ea1d-464f-8cac-375a4fe709c9"
    - from: "Task 6 checkpoint:human-verify"
      to: "user git tag v4.2.1 && git push origin main --tags"
      via: "explicit resume-signal gate; Claude pauses, user acts"
      pattern: "approved"
---

<objective>
Ship Phase 9 as hotfix release v4.2.1. Propagate the version across the 5 canonical files via `./scripts/bump-version.sh 4.2.1`. Write a Keep-a-Changelog 1.1.0 [4.2.1] section in CHANGELOG.md mirroring the [4.1.2] Phase-7 hotfix voice (bold-lead-phrase + en-dash + Phase-NN/D-NN citations + inline code symbols + live-incident forensic anchor per D-09). Create scripts/phase-09/verify.sh + verify.ps1 cross-platform smoke harnesses that mirror the Phase-8 T1-T6 template structurally but swap the T3+ assertions to Phase-9 stale-check absence semantics (SourceClaude UUID session must survive a >=150s stale-check sweep with zero "removing stale session" lines emitted). Gate git tag + push behind a human-verify checkpoint so the user manually signs off before the GoReleaser CI workflow runs.

Purpose: Closes Phase 9 with a deployable v4.2.1 hotfix. Documents the D-01 fix in user-facing language for D-09. Provides a live-daemon smoke harness for the stale-check regression path. Keeps the user in the loop on the irreversible tag + push step (per CLAUDE.md Releasing and the 3-tag-push-limit memory).

Output: 5 version-bumped metadata files, CHANGELOG.md with [4.2.1] entry, two verify scripts in scripts/phase-09/, and a user checkpoint that surfaces the release-ready state for tag + push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-CONTEXT.md
@.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-RESEARCH.md
@.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-PATTERNS.md
@.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-VALIDATION.md
@.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-01-SUMMARY.md
@.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-04-PLAN.md
@CHANGELOG.md
@CLAUDE.md
@scripts/bump-version.sh
@scripts/phase-08/verify.sh
@scripts/phase-08/verify.ps1

<interfaces>
bump-version.sh sed regexes (verified — touches exactly these 5 files):
  main.go                          version = "X.Y.Z" inside var block
  .claude-plugin/plugin.json       "version": "X.Y.Z"
  .claude-plugin/marketplace.json  "version": "X.Y.Z"
  scripts/start.sh                 VERSION="vX.Y.Z"
  scripts/start.ps1                $Version = "vX.Y.Z"

CHANGELOG [4.1.2] voice template (verbatim from CHANGELOG.md:27-33):
  ## [4.1.2] - 2026-04-13
  ### Fixed
  - **Daemon self-termination during long MCP-heavy sessions** — `handlePostToolUse` now updates ...

Phase-8 verify.sh structure is the structural drop-in:
  #!/bin/bash + set -euo pipefail
  pass()/fail() helpers
  LOG_OFFSET=$(wc -c < "$LOG_FILE")
  CLEANUP_IDS array + register_cleanup + trap cleanup EXIT
  log_tail() via dd if=$LOG_FILE bs=1 skip=$LOG_OFFSET

Live-incident forensic anchor (from 09-CONTEXT.md D-09):
  ~/.claude/dsrcode.log 2026-04-18 11:37-11:44
  session_id: 2fe1b32a-ea1d-464f-8cac-375a4fe709c9
  PID: 5692
  elapsed at removal: 2m25s (150s)
  8 x "PID dead but session has recent activity, skipping removal" debug lines
  followed by "removing stale session (PID dead, no recent activity)" at 11:44
  daemon auto-exit ~6 s later

Daemon HTTP endpoints (from server/server.go — unchanged this phase):
  GET  /health                HTTP 200 if daemon alive
  POST /hooks/pre-tool-use    {session_id, tool_name, cwd}
  POST /hooks/session-end     {session_id, reason}  (cleanup)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ./scripts/bump-version.sh 4.2.1 — propagate across 5 canonical files</name>
  <files>main.go, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1</files>
  <read_first>
    - @scripts/bump-version.sh (verify the 5 sed regexes target the right files; confirm idempotence)
    - @main.go (lines around version = "4.2.0" — the var block bump-version.sh rewrites)
    - @.claude-plugin/plugin.json (current "version": "4.2.0")
    - @.claude-plugin/marketplace.json (current "version": "4.2.0")
    - @scripts/start.sh (current VERSION="v4.2.0")
    - @scripts/start.ps1 (current $Version = "v4.2.0")
    - @CLAUDE.md Releasing section (steps 1-3 canonical recipe)
  </read_first>
  <behavior>
    - bump-version.sh is IDEMPOTENT on exact version strings — running it twice yields the same file state.
    - No manual sed edits allowed (Anti-Pattern from 09-PATTERNS.md §4-8).
    - Script does NOT touch CHANGELOG.md (that is Task 2).
    - Script does NOT auto-commit — staging + committing is a separate step.
    - `go build ./...` still green after the version-string rewrite.
  </behavior>
  <action>
From the repo root, run the bump script with the new version:

    ./scripts/bump-version.sh 4.2.1

Expected console output (mirrors Phase-8 Task 1 STEP 2):

    Bumping version to 4.2.1...
      Updated main.go
      Updated .claude-plugin/plugin.json
      Updated .claude-plugin/marketplace.json
      Updated scripts/start.sh
      Updated scripts/start.ps1

    Version bumped to 4.2.1 in 5 files.

    Next steps:
      git add main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1
      git commit -m "chore: bump version to v4.2.1"
      git tag v4.2.1
      git push origin main --tags

IGNORE the `git tag` and `git push` lines the script prints — those are the USER's follow-up per CLAUDE.md Releasing.

Verify propagation with an explicit grep across all 5 files:

    for f in main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1; do
        echo -n "$f: "
        grep -c '4\.2\.1' "$f"
    done

Expect each file to return >= 1.

Run `go build ./...` to confirm the version-string rewrite compiles.

Commit with message: `chore(09-02): bump version to v4.2.1`

Include all 5 propagated files in the stage: `git add main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1`
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; grep -c 'version = "4.2.1"' main.go &amp;&amp; grep -c '"version": "4.2.1"' .claude-plugin/plugin.json &amp;&amp; grep -c '"version": "4.2.1"' .claude-plugin/marketplace.json &amp;&amp; grep -c 'VERSION="v4.2.1"' scripts/start.sh &amp;&amp; grep -c 'Version = "v4.2.1"' scripts/start.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'version = "4.2.1"' main.go` == 1
    - `grep -c '"version": "4.2.1"' .claude-plugin/plugin.json` == 1
    - `grep -c '"version": "4.2.1"' .claude-plugin/marketplace.json` == 1
    - `grep -c 'VERSION="v4.2.1"' scripts/start.sh` == 1
    - `grep -c 'Version = "v4.2.1"' scripts/start.ps1` == 1
    - No .bak files leftover: `! ls main.go.bak 2>/dev/null && ! ls scripts/start.sh.bak 2>/dev/null`
    - Build green post-bump: `go build ./...` exits 0
    - Diff scope matches 5 files: `git diff --name-only --cached | sort` equals the 5 canonical files only
  </acceptance_criteria>
  <done>Version 4.2.1 propagated idempotently across 5 files; build green; commit `chore(09-02): bump version to v4.2.1` made.</done>
  <requirements>D-06</requirements>
  <mcp_round>
PRE: sequential-thinking (confirm bump script works on Git Bash for Windows, sed -i.bak idiom already tested in Phase 7/8) + context7 (sed regex for var blocks — verify no edge case with leading tab) + exa (any recent bump-version.sh regressions or idempotence complaints) + crawl4ai (optional).
POST: sequential-thinking (verify all 5 file greps return 1 exactly; confirm go build is green) + re-run exa/context7 only if sed behavior surprises.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG.md [4.2.1] section with live-incident forensic anchor (D-09)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - @CHANGELOG.md lines 1-40 (current state — [Unreleased] at line 8, [4.2.0] at line 10, [4.1.2] at line 27)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-CONTEXT.md D-09 (forensic anchor content: session 2fe1b32a + PID 5692 + 2m25s + 8x skipping-removal)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-PATTERNS.md §"3) CHANGELOG.md — MODIFY" (voice template + insert-position details)
    - @.planning/phases/07-fix-daemon-auto-exit-bugs-pid-dead-check-mcp-activity-tracki/07-01-PLAN.md (Phase-7 bullet style: bold-lead + en-dash + D-NN citation + inline code)
  </read_first>
  <behavior>
    - Insert the `## [4.2.1] - 2026-04-18` section at line 10 — immediately after `## [Unreleased]` (line 8) and immediately before `## [4.2.0]`. Section ordering is load-bearing (Pitfall 4 in 09-RESEARCH.md).
    - Voice mirrors [4.1.2] hotfix entry: each bullet begins with `- **Bold-lead phrase**`, uses an en-dash to separate the summary from detail, cites the Phase + D-NN IDs in parentheses at bullet end, references file paths as inline code.
    - Include the verbatim live-incident forensic anchor (UUID `2fe1b32a-ea1d-464f-8cac-375a4fe709c9`, wrapper PID 5692, 8x debug lines, 2m25s elapsed, 2026-04-18 11:37-11:44 log timestamp range).
    - Three subsections: Fixed (D-01 guard expansion), Changed (D-08 godoc extension), Added (D-05 regression tests + D-06 verify harness).
  </behavior>
  <action>
Open `CHANGELOG.md`. Confirm the insert anchor by reading lines 1-12:
- Line 8: `## [Unreleased]`
- Line 9: blank
- Line 10: `## [4.2.0] - 2026-04-16`

Insert the following block so it occupies lines 10-18 (approximately) and pushes `## [4.2.0]` downward. The block starts with a blank line to preserve the spacing idiom already used between `[Unreleased]` and `[4.2.0]`:

    (blank line)
    ## [4.2.1] - 2026-04-18

    ### Fixed
    - **Daemon no longer auto-exits during long-running UUID-sourced Claude Code sessions** — `session/stale.go:41` guard now also skips the PID-liveness check when `Source == SourceClaude` (Phase 9, D-01). In production, UUID session IDs arrive via the HTTP hook path carrying the wrapper-launcher PID (`start.sh`/`start.ps1`) which exits within seconds, making `IsPidAlive(wrapperPID)` a false-negative for Claude-process liveness. Live incident `~/.claude/dsrcode.log` 2026-04-18 11:37-11:44: session `2fe1b32a-ea1d-464f-8cac-375a4fe709c9`, wrapper PID 5692, 8x `"PID dead but session has recent activity, skipping removal"` debug lines before removal at 2m25s — daemon auto-exit. The Phase-7 guard (Bug #1) covered only `SourceHTTP`; Phase 9 extends it to the second hook-delivered source. The 30-minute `removeTimeout` backstop still reaches zombie sessions that have stopped sending hooks entirely.

    ### Changed
    - **Extended godoc at `session/stale.go:38-52`** explains why the PID-liveness skip covers both `SourceHTTP` and `SourceClaude`, notes Windows orphan-reparenting absence and Unix `start.sh` parent-chain weakness, and documents the `removeTimeout` backstop contract (Phase 9, D-08).

    ### Added
    - **Three regression tests in `session/stale_test.go`** (Phase 9, D-05): `TestStaleCheckSkipsPidCheckForClaudeSource` (D-03 inverted rename of the Phase-7 PID-source test — SourceClaude + dead PID + 5min-old activity survives), `TestStaleCheckRemovesClaudeSourceAfterRemoveTimeout` (30-minute backstop preservation — SourceClaude + 31min-old activity still removed), `TestStaleCheckSurvivesUuidSourcedLongRunningAgent` (live-incident mirror — session `2fe1b32a-ea1d-464f-8cac-375a4fe709c9`, PID 5692, elapsed 150s), plus `TestStaleCheckEmitsDebugOnGuardSkip` (negative slog assertion — the inner `"PID dead but session has recent activity"` debug line MUST NOT fire for `SourceClaude`, proving the guard skips the entire block, not just the `EndSession` call).
    - **Cross-platform verify harness** at `scripts/phase-09/verify.sh` (bash) and `scripts/phase-09/verify.ps1` (PowerShell) — mirrors the Phase-8 T1..Tn pattern with log-offset + cleanup-on-exit idiom, but swaps the T3+ assertions to stale-check absence semantics (SourceClaude UUID session survives a >=150s stale-check sweep with zero `"removing stale session"` log lines emitted) (Phase 9, D-06).

Write the block literally (without the leading 4-space indent shown above — that indent is only for PLAN display). Each bullet is a single physical line in the file (Markdown line-wraps naturally).

After the insert, verify position with `head -25 CHANGELOG.md` — the first three H2 headings in file order must be `[Unreleased]`, `[4.2.1]`, `[4.2.0]`.

DO NOT touch the CHANGELOG footer link block at the end of the file. Phase 9 does not add a `[4.2.1]: ...compare/...` footer link — keep scope narrow (per 09-PATTERNS.md §3 footer-link note).

Commit with message: `docs(09-02): changelog v4.2.1 with live-incident forensic anchor`
  </action>
  <verify>
    <automated>grep -c '## \[4.2.1\]' CHANGELOG.md &amp;&amp; grep -c '2fe1b32a-ea1d-464f-8cac-375a4fe709c9' CHANGELOG.md &amp;&amp; grep -c 'Phase 9, D-01' CHANGELOG.md &amp;&amp; grep -c 'Phase 9, D-08' CHANGELOG.md &amp;&amp; grep -c 'Phase 9, D-05' CHANGELOG.md &amp;&amp; grep -c 'Phase 9, D-06' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## \[4.2.1\]' CHANGELOG.md` == 1
    - Live-incident anchor present: `grep -c '2fe1b32a-ea1d-464f-8cac-375a4fe709c9' CHANGELOG.md` >= 1
    - PID 5692 anchor present: `grep -c '5692' CHANGELOG.md` >= 1
    - 2m25s elapsed anchor present: `grep -c '2m25s' CHANGELOG.md` >= 1
    - All three subsections under [4.2.1]: `grep -c '### Fixed\|### Changed\|### Added' CHANGELOG.md` >= (previous count + 3)
    - Phase + D-NN citations present: each of `Phase 9, D-01`, `Phase 9, D-08`, `Phase 9, D-05`, `Phase 9, D-06` appears at least once in CHANGELOG.md
    - Section ordering correct: first 3 H2 line numbers from `awk '/## \[/{print NR": "$0}' CHANGELOG.md | head -3` show `[Unreleased]` < `[4.2.1]` < `[4.2.0]` (strictly ascending)
    - Bold-lead style preserved: each Phase-9 bullet starts with `- **`
    - Footer link block untouched: `git diff CHANGELOG.md | grep -c '^-\[4\.1\.0\]:'` == 0
  </acceptance_criteria>
  <done>[4.2.1] section lands between [Unreleased] and [4.2.0] with live-incident forensic anchor, all three subsections, and Phase-9 D-NN citations. Commit `docs(09-02): changelog v4.2.1 with live-incident forensic anchor` made.</done>
  <requirements>D-09</requirements>
  <mcp_round>
PRE: sequential-thinking (decompose CHANGELOG voice match against [4.1.2] template) + context7 (Keep-a-Changelog 1.1.0 — confirm Fixed/Changed/Added ordering) + exa (any recent Keep-a-Changelog voice-drift advice) + crawl4ai (optional — skip if docs already match).
POST: sequential-thinking (verify insert position and all required anchors present; cross-check ordering via awk) + re-run exa if voice seems off.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

<task type="auto">
  <name>Task 3: Create scripts/phase-09/verify.sh (bash T1-T3+ harness with SourceClaude stale-check absence T3)</name>
  <files>scripts/phase-09/verify.sh</files>
  <read_first>
    - @scripts/phase-08/verify.sh (structural template — copy #!/bin/bash + set -euo pipefail + pass/fail helpers + LOG_OFFSET idiom + CLEANUP_IDS trap + log_tail() verbatim)
    - @scripts/start.sh (log path convention: ~/.claude/dsrcode.log)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-PATTERNS.md §"9) scripts/phase-09/verify.sh" (T-swap schema: keep T1+T2, REPLACE T3-T6 with Phase-9 T3 stale-check absence)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-RESEARCH.md §"Pitfall 3" (dsrcode binary PATH precondition — document in header)
  </read_first>
  <behavior>
    - Bash shebang + `set -euo pipefail`.
    - Header comment documents Phase-9 scope, T1-T3 assertions, preconditions (daemon running on 127.0.0.1:19460, `dsrcode` on PATH or invoked as `./dsrcode`, fresh `~/.claude/dsrcode.log`).
    - LOG_OFFSET captured at start so T3 only inspects log bytes written during this run.
    - CLEANUP_IDS + trap cleanup EXIT pattern fires `/hooks/session-end` for every fake session_id POSTed during T3.
    - T1 asserts `dsrcode --version` output contains `4.2.1`.
    - T2 asserts `GET /health` returns HTTP 200.
    - T3 POSTs ONE synthetic UUID-shaped session_id to `/hooks/pre-tool-use`, sleeps 150 seconds (mirrors the live-incident elapsed), asserts `log_tail | grep -c '"removing stale session".*"$UUID_ID"'` equals 0.
    - Script ends with "All Phase 9 verify tests passed (T1..T3)."
    - Exit 0 on all-pass, non-zero on any fail.
    - File mode 0755 (executable).
  </behavior>
  <action>
Create directory + file:

    mkdir -p scripts/phase-09

Write `scripts/phase-09/verify.sh` with the full content below. Copy byte-for-byte the Phase-8 skeleton (shebang, set -euo pipefail, pass/fail, LOG_OFFSET, CLEANUP_IDS trap, log_tail) and then swap in the Phase-9 T-block:

    #!/bin/bash
    # Phase 9 live-daemon verification harness (v4.2.1 hotfix).
    #
    # Validates:
    #   T1 dsrcode binary reports version 4.2.1
    #   T2 /health returns HTTP 200
    #   T3 SourceClaude UUID session survives >=150s stale-check sweep
    #      (0 "removing stale session" lines emitted for the injected UUID)
    #
    # Preconditions:
    #   - Daemon must already be running (start.sh launches it).
    #   - `dsrcode` must be on PATH (or invoke via `DSRCODE_BIN=./dsrcode bash scripts/phase-09/verify.sh`).
    #   - ~/.claude/dsrcode.log must exist (auto-created on first daemon launch).
    #
    # Manual follow-up (not automated — wall-clock >2min MCP-heavy reproduction):
    #   Start Claude Code, initiate an MCP-heavy flow with >2min silence between hooks,
    #   tail ~/.claude/dsrcode.log, assert no "removing stale session" lines for the
    #   UUID session across 5+ minutes of activity.
    #
    # Usage: bash scripts/phase-09/verify.sh
    # Exits 0 on all-pass, non-zero on any assertion failure.

    set -euo pipefail

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    LOG_FILE="${HOME}/.claude/dsrcode.log"
    DAEMON_URL="http://127.0.0.1:19460"
    DSRCODE_BIN="${DSRCODE_BIN:-dsrcode}"

    pass() { echo "PASS: $1"; }
    fail() { echo "FAIL: $1" >&2; exit 1; }

    if [[ ! -f "$LOG_FILE" ]]; then
        fail "log file $LOG_FILE not found — daemon not running?"
    fi
    LOG_OFFSET=$(wc -c < "$LOG_FILE")

    CLEANUP_IDS=()
    CLEANUP_DONE=false
    register_cleanup() {
        CLEANUP_IDS+=("$1")
    }
    cleanup() {
        local rc=$?
        if $CLEANUP_DONE; then
            return $rc
        fi
        CLEANUP_DONE=true
        set +e
        for id in "${CLEANUP_IDS[@]+"${CLEANUP_IDS[@]}"}"; do
            curl -sS -X POST \
                -H "Content-Type: application/json" \
                -d "{\"session_id\":\"$id\",\"reason\":\"verify-09-sh-cleanup\"}" \
                "${DAEMON_URL}/hooks/session-end" \
                >/dev/null 2>&1 || true
        done
        return $rc
    }
    trap cleanup EXIT

    log_tail() {
        dd if="$LOG_FILE" bs=1 skip="$LOG_OFFSET" 2>/dev/null
    }

    # --- T1: Binary version ---
    if ! command -v "$DSRCODE_BIN" >/dev/null 2>&1; then
        fail "T1 dsrcode binary not on PATH (override with DSRCODE_BIN=./dsrcode)"
    fi
    VERSION_OUT=$("$DSRCODE_BIN" --version 2>&1 || true)
    if echo "$VERSION_OUT" | grep -q "4.2.1"; then
        pass "T1 dsrcode --version contains 4.2.1"
    else
        fail "T1 dsrcode --version lacks 4.2.1 — got: $VERSION_OUT"
    fi

    # --- T2: /health ---
    if curl -fsS -o /dev/null "${DAEMON_URL}/health"; then
        pass "T2 /health returned HTTP 200"
    else
        fail "T2 /health did not return 200 (daemon not running or bind changed)"
    fi

    # --- T3: SourceClaude guard holds across >=150s stale-check sweep ---
    # Inject a UUID-shaped session_id so sourceFromID returns SourceClaude, then
    # send no further hooks for 150s. The Phase-9 D-01 guard must skip the
    # PID-liveness branch entirely; zero "removing stale session" lines should
    # appear for this UUID during the sweep window.
    UUID_ID="verify09-$(date +%s)-abc-def-1234-5678-90ab-cdef"
    register_cleanup "$UUID_ID"
    curl -fsS -o /dev/null -X POST \
        -H "Content-Type: application/json" \
        -d "{\"session_id\":\"$UUID_ID\",\"tool_name\":\"Edit\",\"cwd\":\"/tmp/verify09\"}" \
        "${DAEMON_URL}/hooks/pre-tool-use"

    echo "T3 waiting 150s for stale-check ticker to sweep (mirrors incident elapsed)..."
    sleep 150

    T3_REMOVE_COUNT=$(log_tail | grep -c "\"removing stale session\".*\"$UUID_ID\"" || true)
    if [[ "$T3_REMOVE_COUNT" -eq 0 ]]; then
        pass "T3 SourceClaude UUID session survived PID-liveness sweep (0 removals observed)"
    else
        fail "T3 SourceClaude UUID session was removed $T3_REMOVE_COUNT time(s) — v4.2.1 guard regression"
    fi

    echo ""
    echo "All Phase 9 verify tests passed (T1..T3)."

Then mark executable:

    chmod +x scripts/phase-09/verify.sh

Commit with message: `chore(09-02): add scripts/phase-09/verify.sh (bash T1-T3 harness)`
  </action>
  <verify>
    <automated>test -x scripts/phase-09/verify.sh &amp;&amp; head -1 scripts/phase-09/verify.sh | grep -q '^#!/bin/bash' &amp;&amp; grep -q 'set -euo pipefail' scripts/phase-09/verify.sh &amp;&amp; grep -c 'pass "T[1-3]' scripts/phase-09/verify.sh &amp;&amp; grep -q 'verify09-' scripts/phase-09/verify.sh &amp;&amp; grep -q '/hooks/pre-tool-use' scripts/phase-09/verify.sh &amp;&amp; grep -q 'trap cleanup EXIT' scripts/phase-09/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists + executable: `test -x scripts/phase-09/verify.sh`
    - Shebang correct: `head -1 scripts/phase-09/verify.sh` equals `#!/bin/bash`
    - Strict mode: `grep -q 'set -euo pipefail' scripts/phase-09/verify.sh`
    - Three PASS paths present: `grep -c 'pass "T[1-3]' scripts/phase-09/verify.sh` == 3
    - Version check at 4.2.1: `grep -c '4.2.1' scripts/phase-09/verify.sh` >= 2 (header + T1)
    - LOG_OFFSET idiom present: `grep -q 'LOG_OFFSET=\$(wc -c' scripts/phase-09/verify.sh`
    - Cleanup trap present: `grep -q 'trap cleanup EXIT' scripts/phase-09/verify.sh`
    - T3 UUID injection scheme present: `grep -q 'verify09-' scripts/phase-09/verify.sh`
    - T3 assertion uses "removing stale session" negative grep: `grep -q 'grep -c.*removing stale session' scripts/phase-09/verify.sh`
    - Shellcheck lint (if available): `command -v shellcheck >/dev/null && shellcheck scripts/phase-09/verify.sh` exits 0 or emits only style warnings (SC2*-style)
    - Bash parse-only check: `bash -n scripts/phase-09/verify.sh` exits 0
  </acceptance_criteria>
  <done>scripts/phase-09/verify.sh exists, mode 0755, parses with `bash -n`, asserts T1-T3 with SourceClaude stale-check absence as the flagship check. Commit `chore(09-02): add scripts/phase-09/verify.sh (bash T1-T3 harness)` made.</done>
  <requirements>D-06</requirements>
  <mcp_round>
PRE: sequential-thinking (decompose T-swap from Phase-8 to Phase-9; keep cleanup trap verbatim) + context7 (bash set -euo pipefail edge cases with `|| true` on piped grep counts) + exa (Git-Bash-on-Windows portability of `date +%s` + `dd skip`) + crawl4ai (optional).
POST: sequential-thinking (verify shellcheck + bash -n pass) + exa re-query if any Windows-specific idiom fails.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

<task type="auto">
  <name>Task 4: Create scripts/phase-09/verify.ps1 (PowerShell parity)</name>
  <files>scripts/phase-09/verify.ps1</files>
  <read_first>
    - @scripts/phase-08/verify.ps1 (structural template — copy #Requires 5.1 + Invoke-Test helper + Count-LogMatches + logOffset + finally cleanup verbatim)
    - @scripts/phase-09/verify.sh (Task 3 output — semantic T-block parity)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-PATTERNS.md §"10) scripts/phase-09/verify.ps1" (T-swap schema)
  </read_first>
  <behavior>
    - `#Requires -Version 5.1` header.
    - Exports `verify-results.json` with pass/fail counts for downstream consumption (match Phase-8 pattern).
    - Uses `$env:USERPROFILE\.claude\dsrcode.log` as log path.
    - Handles missing log file cleanly (exit 1 with message).
    - T1, T2, T3 match semantics of `verify.sh` exactly.
    - T3 uses `Start-Sleep -Seconds 150` and regex-escapes the UUID in Count-LogMatches.
    - `$script:CleanupIds` list + finally block posts `/hooks/session-end` for each collected ID.
    - Exit 1 on any failure, 0 on all-pass.
  </behavior>
  <action>
Create `scripts/phase-09/verify.ps1` with the following content (based verbatim on scripts/phase-08/verify.ps1 lines 1-85 + 191-208 — only the T-block differs):

    #Requires -Version 5.1
    <#
    .SYNOPSIS
        Phase 9 live-daemon verification harness (v4.2.1 hotfix).

    .DESCRIPTION
        Validates:
          T1 dsrcode binary reports version 4.2.1
          T2 /health returns HTTP 200
          T3 SourceClaude UUID session survives >=150s stale-check sweep
             (0 "removing stale session" lines emitted for the injected UUID)

        Preconditions:
          - Daemon must already be running (start.ps1 launches it).
          - dsrcode.exe must be on PATH.
          - $env:USERPROFILE\.claude\dsrcode.log must exist.

    .EXAMPLE
        powershell -ExecutionPolicy Bypass -File scripts\phase-09\verify.ps1
    #>

    [CmdletBinding()]
    param()

    $ErrorActionPreference = 'Continue'

    $script:results  = [ordered]@{}
    $script:passCount = 0
    $script:failCount = 0

    $DaemonUrl = 'http://127.0.0.1:19460'
    $LogFile   = Join-Path $env:USERPROFILE '.claude\dsrcode.log'

    function Invoke-Test {
        param(
            [string]$Id,
            [string]$Description,
            [scriptblock]$Test
        )
        Write-Host -NoNewline "[$Id] $Description ... "
        try {
            $result = & $Test
            if ($result -is [bool] -and $result) {
                Write-Host "PASS" -ForegroundColor Green
                $script:passCount++
                $script:results[$Id] = 'PASS'
                return $true
            } else {
                Write-Host "FAIL" -ForegroundColor Red
                $script:failCount++
                $script:results[$Id] = 'FAIL'
                return $false
            }
        } catch {
            Write-Host "ERROR: $($_.Exception.Message)" -ForegroundColor Red
            $script:failCount++
            $script:results[$Id] = "ERROR: $($_.Exception.Message)"
            return $false
        }
    }

    if (-not (Test-Path -Path $LogFile)) {
        Write-Host "FAIL: log file $LogFile not found -- daemon not running?" -ForegroundColor Red
        exit 1
    }
    $script:logOffset = (Get-Item $LogFile).Length

    function Get-NewLogBytes {
        $fs = [System.IO.File]::Open($LogFile, 'Open', 'Read', 'ReadWrite')
        try {
            $null = $fs.Seek($script:logOffset, 'Begin')
            $sr = New-Object System.IO.StreamReader($fs)
            return $sr.ReadToEnd()
        } finally {
            $fs.Close()
        }
    }

    function Count-LogMatches {
        param([string]$Pattern)
        $text = Get-NewLogBytes
        if ([string]::IsNullOrEmpty($text)) { return 0 }
        return ([regex]::Matches($text, $Pattern)).Count
    }

    function Invoke-HookPost {
        param(
            [string]$Path,
            [string]$Body
        )
        Invoke-WebRequest -Method Post -Uri ($DaemonUrl + $Path) `
            -ContentType 'application/json' -Body $Body `
            -UseBasicParsing -TimeoutSec 5 -ErrorAction Stop | Out-Null
    }

    $script:CleanupIds = New-Object 'System.Collections.Generic.List[string]'

    try {

    # --- T1: Binary version ---
    Invoke-Test -Id 'T1' -Description 'dsrcode --version reports 4.2.1' -Test {
        $cmd = Get-Command -Name 'dsrcode' -ErrorAction SilentlyContinue
        if (-not $cmd) { return $false }
        $output = & dsrcode --version 2>&1 | Out-String
        return ($output -match '4\.2\.1')
    }

    # --- T2: /health ---
    Invoke-Test -Id 'T2' -Description 'HTTP 200 from /health' -Test {
        try {
            $resp = Invoke-WebRequest -Uri ($DaemonUrl + '/health') -UseBasicParsing -TimeoutSec 3 -ErrorAction Stop
            return ($resp.StatusCode -eq 200)
        } catch {
            return $false
        }
    }

    # --- T3: SourceClaude UUID session survives >=150s stale-check sweep ---
    Invoke-Test -Id 'T3' -Description 'SourceClaude UUID session survives PID-liveness sweep' -Test {
        $uuid = "verify09-$(Get-Date -Format 'yyyyMMddHHmmss')-abc-def-1234-5678-90ab-cdef"
        $script:CleanupIds.Add($uuid)
        Invoke-HookPost -Path '/hooks/pre-tool-use' `
            -Body "{`"session_id`":`"$uuid`",`"tool_name`":`"Edit`",`"cwd`":`"/tmp/verify09`"}"

        Write-Host 'T3 waiting 150s for stale-check ticker to sweep (mirrors incident elapsed)...'
        Start-Sleep -Seconds 150

        $pattern = '"removing stale session".*"' + [regex]::Escape($uuid) + '"'
        $removes = Count-LogMatches $pattern
        Write-Verbose "T3 observed $removes removals for $uuid (expect 0)"
        return ($removes -eq 0)
    }

    # --- Summary ---
    Write-Host ''
    Write-Host "Phase 9 verify: $script:passCount passed, $script:failCount failed"

    $summaryPath = Join-Path $PSScriptRoot 'verify-results.json'
    $summary = @{
        phase     = '9'
        timestamp = (Get-Date).ToString('o')
        passCount = $script:passCount
        failCount = $script:failCount
        results   = $script:results
    } | ConvertTo-Json -Depth 4
    Set-Content -Path $summaryPath -Value $summary -Encoding UTF8

    if ($script:failCount -gt 0) { exit 1 }
    exit 0

    }
    finally {
        foreach ($id in $script:CleanupIds) {
            try {
                $body = "{`"session_id`":`"$id`",`"reason`":`"verify-09-ps1-cleanup`"}"
                Invoke-WebRequest -Method Post -Uri ($DaemonUrl + '/hooks/session-end') `
                    -ContentType 'application/json' -Body $body `
                    -UseBasicParsing -TimeoutSec 3 -ErrorAction Stop | Out-Null
            } catch {
                # Swallow — cleanup must never abort.
            }
        }
    }

Do NOT `chmod +x` — `.ps1` executes via the PowerShell host.

Commit with message: `chore(09-02): add scripts/phase-09/verify.ps1 (PowerShell parity)`
  </action>
  <verify>
    <automated>test -f scripts/phase-09/verify.ps1 &amp;&amp; head -1 scripts/phase-09/verify.ps1 | grep -q '#Requires -Version 5.1' &amp;&amp; grep -q 'function Invoke-Test' scripts/phase-09/verify.ps1 &amp;&amp; grep -c "Invoke-Test -Id 'T[1-3]'" scripts/phase-09/verify.ps1 &amp;&amp; grep -q '4\\\\.2\\\\.1' scripts/phase-09/verify.ps1 &amp;&amp; grep -q 'verify09-' scripts/phase-09/verify.ps1</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/phase-09/verify.ps1`
    - First line shebang-equivalent: `head -1 scripts/phase-09/verify.ps1` contains `#Requires -Version 5.1`
    - Invoke-Test helper defined: `grep -q 'function Invoke-Test' scripts/phase-09/verify.ps1`
    - Three Invoke-Test calls (T1/T2/T3): `grep -c \"Invoke-Test -Id 'T[1-3]'\" scripts/phase-09/verify.ps1` == 3
    - T1 regex matches 4.2.1: `grep -q 'match .4\\\\\\.2\\\\\\.1.' scripts/phase-09/verify.ps1` (backslash escaping per PowerShell regex)
    - Log path uses USERPROFILE: `grep -q 'USERPROFILE' scripts/phase-09/verify.ps1`
    - finally-block cleanup present: `grep -q 'finally' scripts/phase-09/verify.ps1` AND `grep -q '/hooks/session-end' scripts/phase-09/verify.ps1`
    - PowerShell parse check (if pwsh or powershell available):
      `pwsh -NoProfile -Command "& { try { [scriptblock]::Create((Get-Content -Raw scripts/phase-09/verify.ps1)) | Out-Null; exit 0 } catch { Write-Host \\$_; exit 1 } }"` exits 0
      (fallback: skip the parse check if neither pwsh nor powershell available, treat as documented precondition)
    - verify-results.json export present: `grep -q 'verify-results.json' scripts/phase-09/verify.ps1`
  </acceptance_criteria>
  <done>scripts/phase-09/verify.ps1 exists, declares PowerShell 5.1 requirement, parses cleanly, exercises T1-T3 with the same semantics as verify.sh, and exports verify-results.json. Commit `chore(09-02): add scripts/phase-09/verify.ps1 (PowerShell parity)` made.</done>
  <requirements>D-06</requirements>
  <mcp_round>
PRE: sequential-thinking (confirm T-block parity with verify.sh; consider Windows-specific edge cases like log file locking — File.Open with FileShare.ReadWrite is the fix, already present in Phase-8 analog) + context7 (PowerShell Invoke-WebRequest + Start-Sleep; regex::Escape for session_id) + exa (pwsh vs powershell 5.1 incompatibility notes for this harness) + crawl4ai (optional).
POST: sequential-thinking (verify script parses + T3 uses regex::Escape to prevent UUID regex injection issues) + exa re-query if an edge case surfaces.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

<task type="auto">
  <name>Task 5: Run live verify harness against freshly rebuilt v4.2.1 daemon</name>
  <files>(no repo files modified — validation only)</files>
  <read_first>
    - @scripts/phase-09/verify.sh (Task 3 output)
    - @scripts/phase-09/verify.ps1 (Task 4 output)
    - @scripts/start.sh + @scripts/start.ps1 (daemon launch procedure)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-VALIDATION.md §"Per-Task Verification Map" task 09-02-05
  </read_first>
  <behavior>
    - No git commit from this task (validation only; evidence goes into 09-02-SUMMARY.md).
    - Rebuild daemon locally from current HEAD (which includes Phase-9 Plan 01 + Plan 02 Tasks 1-4).
    - Stop any currently-running daemon (clean slate).
    - Launch via start.sh (Unix/Git Bash) OR start.ps1 (Windows PowerShell). Do NOT launch both.
    - Run the matching verify harness. All T1..T3 must PASS.
    - On FAIL, capture the last 100 lines of `~/.claude/dsrcode.log` into /tmp/09-02-05-fail.log for debugging; do NOT retry blindly — loop back to the owning task (1-4).
  </behavior>
  <action>
Rebuild the daemon locally so the binary under test is v4.2.1:

    go build -o dsrcode .

Stop any existing daemon instance (pick ONE branch per platform):

Unix/macOS/Git Bash:

    bash scripts/stop.sh || true
    bash scripts/start.sh

Windows PowerShell:

    powershell -ExecutionPolicy Bypass -File scripts/stop.ps1
    powershell -ExecutionPolicy Bypass -File scripts/start.ps1

Wait 3 seconds for the daemon to bind port 19460 and start logging:

    sleep 3   # or Start-Sleep -Seconds 3 in PowerShell

Confirm the daemon is alive:

    curl -fsS http://127.0.0.1:19460/health

Expected: `ok` (HTTP 200).

Run the matching verify harness:

Unix/macOS/Git Bash:

    bash scripts/phase-09/verify.sh

Windows PowerShell:

    powershell -ExecutionPolicy Bypass -File scripts/phase-09/verify.ps1

Wait for all three PASS lines (~152 s total wall-clock due to the T3 150-second sleep):

    PASS: T1 dsrcode --version contains 4.2.1
    PASS: T2 /health returned HTTP 200
    PASS: T3 SourceClaude UUID session survived PID-liveness sweep (0 removals observed)

    All Phase 9 verify tests passed (T1..T3).

If ANY test fails:
1. Capture log context: `tail -100 ~/.claude/dsrcode.log > /tmp/09-02-05-fail.log`
2. Identify which test failed + which log line contradicts the expectation.
3. Loop back to the owning task: T1 fail → Task 1 (version propagation); T2 fail → environment (port 19460 free? daemon binary matches HEAD?); T3 fail → Plan 09-01 regression in the guard (re-run `go test -race ./session/...`).
4. DO NOT paper over a fail by tweaking the verify script.

No commit for this task — evidence is recorded in 09-02-SUMMARY.md after Task 6 approval.
  </action>
  <verify>
    <automated>echo "manual validation task — verify.sh/ps1 output captured in 09-02-SUMMARY.md"</automated>
  </verify>
  <acceptance_criteria>
    - `go build -o dsrcode .` exits 0 (binary rebuilt from HEAD-includes-Phase-9)
    - Daemon `/health` returns HTTP 200 after restart: `curl -fsS http://127.0.0.1:19460/health`
    - T1 PASS: `dsrcode --version` output contains `4.2.1`
    - T2 PASS: `/health` returns 200
    - T3 PASS: 0 "removing stale session" log lines observed for the synthetic verify09-* UUID during the 150s sweep window
    - Script exit code is 0 on success
    - No fail captured in /tmp/09-02-05-fail.log (file should not be written on success)
  </acceptance_criteria>
  <done>verify.sh OR verify.ps1 reports T1..T3 all PASS against a freshly rebuilt v4.2.1 daemon. Output captured verbatim for 09-02-SUMMARY.md.</done>
  <requirements>D-06</requirements>
  <mcp_round>
PRE: sequential-thinking (confirm rebuild+restart+verify ordering is load-bearing; T3 sleep is 150s wall-clock — user expects ~3 minutes) + context7 (go build flags — no -race on the binary, only on tests) + exa (any recent verify-harness false-positives to watch for) + crawl4ai (optional).
POST: sequential-thinking (record PASS output verbatim; if fail, diagnose BEFORE retrying) + exa if a non-obvious failure mode shows up.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Human-verify release artefacts + user-owned git tag v4.2.1 + push</name>
  <files>CHANGELOG.md, main.go, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1, scripts/phase-09/verify.sh, scripts/phase-09/verify.ps1, session/stale.go, session/stale_test.go</files>
  <read_first>
    - @CLAUDE.md Releasing section (user's canonical tag + push recipe)
    - @.planning/phases/08-presence-rate-limit-coalescer-stop-drop-on-skip-token-bucket/08-04-PLAN.md Task 4 (human-verify pattern to mirror)
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-CONTEXT.md Releasing constraints + 3-tag-push-limit memory
    - @.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-RESEARCH.md §"Pitfall 5" (user's manual git tag step)
  </read_first>
  <behavior>
    - Claude PAUSES here and presents <what-built>, <how-to-verify>, <resume-signal>.
    - Claude MUST NOT run `git tag v4.2.1` or `git push origin main --tags` autonomously.
    - Checkpoint resumes ONLY on explicit user signal ("approved" or equivalent).
    - On rejection, Claude identifies the concern and loops back to the offending task (Tasks 1-5).
    - After approval, Claude writes the 09-02-SUMMARY.md and updates STATE.md + ROADMAP.md.
  </behavior>
  <action>
Present the state below to the user and wait for their explicit "approved" resume-signal. Claude MUST NOT execute `git tag` or `git push --tags` commands — per CLAUDE.md Releasing and the 3-tag-push-limit memory, that step is exclusively the USER's.

On approval: mark Task 6 complete, then write 09-02-SUMMARY.md and update STATE.md + ROADMAP.md.
On rejection: fix the user-described issue in the owning task (1-5), then re-present this checkpoint.
  </action>
  <what-built>
Phase 9 is feature-complete:
- Plan 09-01: session/stale.go guard widened (SourceClaude also skipped), 15-line godoc landed, session/stale_test.go renamed 1 test + added 3 (4 total Phase-9 tests + 1 preserved Phase-7 HTTP sibling). go test -race ./... green.
- Plan 09-02 Task 1: Version 4.2.1 propagated across main.go, .claude-plugin/plugin.json, .claude-plugin/marketplace.json, scripts/start.sh, scripts/start.ps1 via scripts/bump-version.sh.
- Plan 09-02 Task 2: CHANGELOG.md has a complete [4.2.1] entry with live-incident forensic anchor (session 2fe1b32a-ea1d-464f-8cac-375a4fe709c9, PID 5692, 2m25s elapsed).
- Plan 09-02 Task 3 + 4: scripts/phase-09/verify.sh (bash, mode 0755) + verify.ps1 (PowerShell 5.1+) created with T1-T3 SourceClaude stale-check absence harness.
- Plan 09-02 Task 5: Live verify harness run against freshly rebuilt v4.2.1 daemon — T1, T2, T3 all PASS.
- Full test suite go test -race ./... green across all packages.
  </what-built>
  <how-to-verify>
1. Read the [4.2.1] CHANGELOG entry aloud — does it describe the user-visible win clearly?
   - Daemon no longer auto-exits during long-running UUID-sourced Claude Code sessions
   - Mentions session/stale.go guard + wrapper PID + HTTP hook path + Windows orphan + Unix start.sh
   - Cites Phase 9 + D-NN IDs
   - Quotes the live incident log excerpt (session 2fe1b32a + PID 5692 + 2m25s + 8x skipping-removal)

2. Confirm version propagation across 5 files:
   grep -l '4\.2\.1' main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1
   All 5 paths should appear.

3. Run the full test suite:
   go test ./... -race -count=1 -v
   Expect all packages green (session + coalescer + server + everything else).

4. If not already done by Plan 09-02 Task 5: rebuild + restart + run verify harness for your platform. All T1..T3 must PASS.

5. Optional: Reproduce the live-incident scenario manually with Claude Code.
   - Start Claude Code, trigger an MCP-heavy chain (sequential-thinking + multi-step tool use).
   - Leave Claude idle for >5 minutes.
   - Tail ~/.claude/dsrcode.log.
   - Assert: no "removing stale session" lines emitted for your UUID session across the 5min idle window.
   - Assert: daemon is still running (/health returns 200).

6. Once satisfied, YOU (the user) run the tag + push per CLAUDE.md Releasing step 2:
     git add main.go .claude-plugin/plugin.json .claude-plugin/marketplace.json scripts/start.sh scripts/start.ps1 CHANGELOG.md scripts/phase-09/ session/stale.go session/stale_test.go
     git commit -m "chore: bump version to v4.2.1"
     git tag v4.2.1
     git push origin main --tags

   GoReleaser CI will auto-build the 5-platform binaries and cut a GitHub Release.

Claude MUST NOT execute the tag + push commands. 3-tag-push-limit memory applies (we are pushing 1 new tag, well under the 3-tag ceiling — still, tag ownership stays with the user).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 9 complete, or describe what needs fixing. Only after "approved" will this plan's tasks mark done — the actual git tag + push remains the user's responsibility per CLAUDE.md Releasing and the 3-tag-push-limit memory.</resume-signal>
  <verify>
    <automated>go test ./... -race -count=1 &amp;&amp; grep -c 'version = "4.2.1"' main.go &amp;&amp; grep -c '## \[4.2.1\]' CHANGELOG.md &amp;&amp; test -x scripts/phase-09/verify.sh &amp;&amp; test -f scripts/phase-09/verify.ps1</automated>
  </verify>
  <acceptance_criteria>
    - User has explicitly typed "approved" after reviewing all artefacts listed in <what-built>.
    - Full `go test -race ./...` re-confirmed green at checkpoint time.
    - `grep -c 'version = "4.2.1"' main.go` == 1
    - `grep -c '## \[4.2.1\]' CHANGELOG.md` == 1
    - `test -x scripts/phase-09/verify.sh` exits 0
    - `test -f scripts/phase-09/verify.ps1` exits 0
    - Claude has NOT executed `git tag v4.2.1` or `git push origin main --tags` (verify: `git tag --list 'v4.2.1'` returns empty from Claude's side; user's manual tag push comes AFTER this checkpoint)
    - User acknowledges the tag+push is their follow-up step
  </acceptance_criteria>
  <done>User typed "approved" after reviewing CHANGELOG, confirming version propagation, seeing the full test suite green, and running the live verify harness with T1..T3 all PASS. The actual git tag v4.2.1 + git push origin main --tags is the user's manual step performed OUTSIDE this plan scope. On approval, Claude writes 09-02-SUMMARY.md + updates STATE.md + ROADMAP.md.</done>
  <requirements>D-06, D-09</requirements>
  <mcp_round>
PRE: sequential-thinking (confirm checkpoint framing matches Phase-8 08-04 Task-4 precedent; confirm Claude does not automate git tag) + context7 (git tag semantics — confirm GoReleaser trigger on tag-push) + exa (any recent GoReleaser CI regressions for 5-platform builds) + crawl4ai (optional).
POST: sequential-thinking (user approval captured; SUMMARY written; STATE + ROADMAP updated) + exa if CI surprises on tag push.
Document both rounds in 09-SUMMARY.md §MCP-Rounds.
  </mcp_round>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/bump-version.sh to 5 target files | Shell script modifying version strings via sed; trusted (repo-owned). |
| scripts/phase-09/verify.* to live daemon | Exercises HTTP endpoints on 127.0.0.1:19460; read-only w.r.t. repo state; cleanup posts `/hooks/session-end` for injected fake IDs. |
| CHANGELOG.md to downstream release consumers | Published as release notes; must accurately describe user-facing changes with no secret leakage. |
| Human verification checkpoint to user git tag + push | User-controlled; Claude does NOT execute tag/push autonomously (3-tag-push-limit memory + CLAUDE.md Releasing). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | bump-version.sh updates some files but misses others (partial rename) | mitigate | Task 1 verifies all 5 files contain `4.2.1` via explicit grep loop. Any missing file fails the acceptance criterion. |
| T-09-02-02 | Information Disclosure | CHANGELOG accidentally references internal file paths or implementation secrets | accept | CHANGELOG content is user-facing by design; reviewed in Task 6 checkpoint. No secrets (API keys, tokens, env vars) appear in the draft text — only file paths and session UUIDs. |
| T-09-02-03 | Denial of Service | verify.sh / verify.ps1 infinite-looping against daemon | mitigate | Both scripts have explicit `sleep 150` / `Start-Sleep -Seconds 150` bounds. Total runtime ~2.5 min. No retry loops. |
| T-09-02-04 | Denial of Service | verify scripts flooding daemon with hook requests during T3 | accept | 1 POST per test, well below any reasonable rate limit. |
| T-09-02-05 | Elevation of Privilege | git tag + push executed autonomously without user approval | mitigate | Task 6 is a `checkpoint:human-verify` gate. Explicit instruction: "Claude MUST NOT run the tag + push commands autonomously." 3-tag-push-limit memory reinforces this. |
| T-09-02-06 | Spoofing | Mis-dated CHANGELOG entry (wrong date) | mitigate | Task 2 hard-codes the entry date `2026-04-18` matching the live-incident log; Task 6 checkpoint review catches drift. |
| T-09-02-07 | Information Disclosure | Live incident log excerpt in CHANGELOG exposes a real session UUID | accept | Session UUIDs are randomly generated by Claude Code and do not identify the user personally. Log excerpt is from the user's own local machine (already quoted with consent). Same disclosure posture as Phase-7 [4.1.2] which cited upstream issue numbers. |
</threat_model>

<verification>
- `git status` shows 5 version-bumped files + CHANGELOG.md + 2 scripts/phase-09/ files + (from Plan 09-01) session/stale.go + session/stale_test.go all modified/new.
- `go build ./...` succeeds (post-version-bump).
- `go test ./... -race -count=1` all green.
- Live daemon: `bash scripts/phase-09/verify.sh` (or verify.ps1) T1-T3 all PASS.
- User manually approves via checkpoint before any git tag operation.
- `git tag --list 'v4.2.1'` from Claude's execution context returns empty — user performs tag + push outside this plan's scope.
</verification>

<success_criteria>
- [ ] bump-version.sh 4.2.1 propagated to main.go, plugin.json, marketplace.json, start.sh, start.ps1
- [ ] CHANGELOG.md has a [4.2.1] section with Fixed + Changed + Added subsections matching [4.1.2] voice
- [ ] CHANGELOG [4.2.1] Fixed bullet cites live-incident forensic anchor (2fe1b32a + PID 5692 + 2m25s)
- [ ] scripts/phase-09/verify.sh exists, executable, has T1-T3 with SourceClaude stale-check absence as the flagship
- [ ] scripts/phase-09/verify.ps1 exists, parses cleanly, has T1-T3 parity
- [ ] Live verify harness run against freshly rebuilt v4.2.1 daemon — all T1..T3 PASS
- [ ] go test ./... -race -count=1 green across the whole repo
- [ ] Human verification checkpoint Task 6 executed; user explicit signoff before tag/push
- [ ] Locked decisions D-06 (release set), D-09 (CHANGELOG forensic anchor) BOTH implemented
- [ ] Claude did NOT execute `git tag v4.2.1` or `git push origin main --tags` anywhere in this plan
</success_criteria>

<output>
After user approval at Task 6 checkpoint, create `.planning/phases/09-fix-stale-session-false-positive-for-uuid-sourced-claude-ses/09-02-SUMMARY.md` covering:
- Commits made (expected 4: bump, changelog, verify.sh, verify.ps1)
- T1-T3 live-verify results (verbatim console output from Task 5)
- Final version propagation confirmation (grep output from Task 1)
- Any deviations + rationale
- MCP-Rounds: PRE+POST round per task (6 tasks x 2 rounds = 12 rounds minimum) documented in a dedicated section
- Handoff: Phase 9 complete. Next action is USER's: `git tag v4.2.1 && git push origin main --tags` per CLAUDE.md Releasing (GoReleaser CI handles the 5-platform binary build + GitHub Release on tag push).
- Also update .planning/STATE.md (progress → 10/10 phases planned, current position Phase 9 complete) and .planning/ROADMAP.md Phase 9 status → Complete.
</output>
