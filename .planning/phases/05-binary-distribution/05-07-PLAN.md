---
phase: 05-binary-distribution
plan: 07
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - _skills/doctor/SKILL.md
  - _skills/update/SKILL.md
  - _skills/setup/SKILL.md
  - _skills/log/SKILL.md
autonomous: true
requirements:
  - DIST-32
  - DIST-33
  - DIST-31

must_haves:
  truths:
    - "All 4 skills reference dsrcode binary paths, not cc-discord-presence"
    - "All 4 skills reference dsrcode-config.json, not discord-presence-config.json"
    - "Update skill references StrainReviews/dsrcode, not DSR-Labs/cc-discord-presence"
    - "Log skill references dsrcode.log, not discord-presence.log"
    - "Doctor skill references dsrcode.pid, not discord-presence.pid"
  artifacts:
    - path: "_skills/doctor/SKILL.md"
      provides: "Doctor diagnostics with dsrcode paths"
      contains: "dsrcode"
    - path: "_skills/update/SKILL.md"
      provides: "Update skill with StrainReviews/dsrcode URLs"
      contains: "StrainReviews/dsrcode"
    - path: "_skills/setup/SKILL.md"
      provides: "Setup wizard with dsrcode paths"
      contains: "dsrcode"
    - path: "_skills/log/SKILL.md"
      provides: "Log viewer with dsrcode.log path"
      contains: "dsrcode.log"
  key_links:
    - from: "_skills/update/SKILL.md"
      to: "GitHub Releases"
      via: "Download URL"
      pattern: "StrainReviews/dsrcode"
---

<objective>
Update all 4 skills (doctor, update, setup, log) with new dsrcode binary paths, config paths, and repository URLs.

Purpose: Skills are used by Claude Code slash commands. They must reference correct file paths and URLs for the renamed project.
Output: 4 updated SKILL.md files
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-binary-distribution/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update doctor and update skills with dsrcode paths and repo URL</name>
  <files>_skills/doctor/SKILL.md, _skills/update/SKILL.md</files>
  <read_first>_skills/doctor/SKILL.md, _skills/update/SKILL.md</read_first>
  <action>
Per DIST-32, DIST-33:

**_skills/doctor/SKILL.md changes:**

Find and replace the following patterns throughout the file:
1. `~/.claude/bin/cc-discord-presence-*` -> Binary path now depends on CLAUDE_PLUGIN_DATA. Update checks to:
   - First check: `ls "${CLAUDE_PLUGIN_DATA:-$HOME/.claude/plugins/data/dsrcode}"/bin/dsrcode* 2>/dev/null`
   - Fallback check: `ls ~/.claude/bin/cc-discord-presence-* 2>/dev/null` (migration period)
2. `~/.claude/bin/cc-discord-presence-* --version` -> `"${CLAUDE_PLUGIN_DATA:-$HOME/.claude/plugins/data/dsrcode}/bin/dsrcode" --version 2>/dev/null`
3. `discord-presence-config.json` -> `dsrcode-config.json` (with note: also check discord-presence-config.json for backward compat)
4. `discord-presence.pid` -> `dsrcode.pid`
5. `discord-presence.log` -> `dsrcode.log`

Keep all diagnostic categories and their structure intact. Only update path references.

**_skills/update/SKILL.md changes:**

Find and replace:
1. `~/.claude/bin/cc-discord-presence-*` -> `"${CLAUDE_PLUGIN_DATA:-$HOME/.claude/plugins/data/dsrcode}/bin/dsrcode"`
2. `DSR-Labs/cc-discord-presence` -> `StrainReviews/dsrcode` (in ALL GitHub API URLs and gh CLI commands)
3. `cc-discord-presence-${OS}-${ARCH}` -> `dsrcode_${VERSION}_${OS}_${ARCH}.tar.gz` (archive name format changed by GoReleaser)
4. `curl -fsSL "https://github.com/DSR-Labs/cc-discord-presence/releases/download/{version}/${BINARY_NAME}"` -> `curl -fsSL "https://github.com/StrainReviews/dsrcode/releases/download/{version}/dsrcode_${VERSION}_${OS}_${ARCH}.tar.gz"`
5. `discord-presence.pid` -> `dsrcode.pid`
6. Add note per DIST-33: "start.sh has priority for version management. This skill is for manual on-demand updates. The lock-file prevents race conditions."
7. Update error messages: `DSR-Labs/cc-discord-presence` -> `StrainReviews/dsrcode`
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && grep -c "dsrcode" _skills/doctor/SKILL.md && grep -c "StrainReviews/dsrcode" _skills/update/SKILL.md && ! grep -q "DSR-Labs/cc-discord-presence" _skills/update/SKILL.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- doctor/SKILL.md contains `dsrcode` binary path references
- doctor/SKILL.md contains `dsrcode-config.json` config path
- doctor/SKILL.md contains `dsrcode.pid` PID path
- doctor/SKILL.md contains `dsrcode.log` log path
- doctor/SKILL.md does NOT contain `cc-discord-presence-*` as primary binary path (only in fallback/migration context)
- update/SKILL.md contains `StrainReviews/dsrcode` (not `DSR-Labs/cc-discord-presence`)
- update/SKILL.md contains `dsrcode.pid` PID path
- update/SKILL.md contains note about start.sh having priority for version management
- update/SKILL.md does NOT contain `DSR-Labs/cc-discord-presence` in any URL
  </acceptance_criteria>
  <done>Doctor and update skills reference dsrcode paths and StrainReviews/dsrcode repo</done>
</task>

<task type="auto">
  <name>Task 2: Update setup and log skills with dsrcode paths</name>
  <files>_skills/setup/SKILL.md, _skills/log/SKILL.md</files>
  <read_first>_skills/setup/SKILL.md, _skills/log/SKILL.md</read_first>
  <action>
Per DIST-32:

**_skills/setup/SKILL.md changes:**

Find and replace:
1. `~/.claude/bin/cc-discord-presence-*` -> `"${CLAUDE_PLUGIN_DATA:-$HOME/.claude/plugins/data/dsrcode}/bin/dsrcode"` (Phase A binary check)
2. `discord-presence-config.json` -> `dsrcode-config.json` (Phase C config write path)
3. `discord-presence.log` -> `dsrcode.log` (error message references)
4. Keep all 7 phases and their structure intact

**_skills/log/SKILL.md changes:**

Find and replace ALL 5 occurrences:
1. `~/.claude/discord-presence.log` -> `~/.claude/dsrcode.log` (5 occurrences per CONTEXT.md)
2. Update the "Log file location" tip: `~/.claude/dsrcode.log (configurable via logFile in config)`
3. Update the clear command: `> ~/.claude/dsrcode.log`
  </action>
  <verify>
    <automated>cd C:/Users/ktown/Projects/cc-discord-presence && grep -c "dsrcode" _skills/setup/SKILL.md && grep -c "dsrcode.log" _skills/log/SKILL.md && ! grep -q "discord-presence.log" _skills/log/SKILL.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
- setup/SKILL.md contains `dsrcode` binary path in Phase A
- setup/SKILL.md contains `dsrcode-config.json` config path in Phase C
- setup/SKILL.md contains `dsrcode.log` in error references
- setup/SKILL.md does NOT contain `cc-discord-presence-*` as primary binary path
- log/SKILL.md contains `dsrcode.log` (all 5 occurrences replaced)
- log/SKILL.md does NOT contain `discord-presence.log` anywhere
- `grep -c "discord-presence" _skills/log/SKILL.md` returns 0
  </acceptance_criteria>
  <done>Setup and log skills reference dsrcode paths throughout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Skills are markdown documentation consumed by Claude Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | I (Information Disclosure) | Skill file paths | accept | Paths reference user's own home directory. No secrets exposed. Skills are already public in the plugin repo. |
</threat_model>

<verification>
1. `grep -r "discord-presence" _skills/log/SKILL.md` returns nothing
2. `grep -r "DSR-Labs/cc-discord-presence" _skills/` returns nothing
3. `grep -c "dsrcode" _skills/doctor/SKILL.md` returns positive count
4. `grep -c "StrainReviews/dsrcode" _skills/update/SKILL.md` returns positive count
</verification>

<success_criteria>
All 4 skills reference correct dsrcode binary paths, dsrcode-* runtime file paths, and StrainReviews/dsrcode repository URL. No references to old cc-discord-presence binary names or DSR-Labs repo URL remain.
</success_criteria>

<output>
After completion, create `.planning/phases/05-binary-distribution/05-07-SUMMARY.md`
</output>
