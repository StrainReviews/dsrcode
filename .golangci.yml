# golangci-lint configuration for CI
# Run: golangci-lint run ./...
run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
    - typecheck

issues:
  exclude-use-default: true
